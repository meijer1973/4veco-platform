# §224 author publication and actual scope

2026-09-06; paragraph_224_builder; BOOK2-TEXTBOOK-PRODUCTION-1-224-BUILD.
This is an author review-ready packet, not a review or acceptance decision.

## Actual committed payload

Own paired root C:/wt/book2-224-build-20260906, branch in both repositories
agent/book2-224-build-20260906. Normally claimed by this actual actor, without
override. Exact source roots are P e42c2b276354aeb1eb903bfb480a5dad27d898b2 and
L 8a3d4018ad6a5082449a17c59f991cbdc93fbb62; unrelated root work was not imported.

Actual substantive evidence payload P d3e9551e4025f007e24a15982da7cdd7cd58ddd7;
final native output L 994657c8901729fcb138493be4e8c2e9590081bd. Source commit
9acf684b78c42a5afbcb1253a7e9cd7711bdf7ab and controller commit
0e2349ecf50e817482bf2f5c1d6d5aedc32c9323 are immutable ancestors, not future
hash placeholders. The final source/output/proof bindings are unchanged.

The result report raw SHA256 is
4a83e82cb2a90a33f0b380a974299410912e75301aff1a2f8243d99732c4dd92.
Scope JSON raw SHA256 is
3c8d72e4998f8ec80a7f949b71459d23f4baec3d20286246ee770b01219cc7ae.
Scope commands bind the actual payload, not a working-tree or artificial anchor.
Their full command lines, exits, stdout/stderr/raw digests, every changed
file's current and committed raw hash, and baseline-custody result are stored.

## Scope and whitespace truth

| Actual scope | Native result | Categories; UNKNOWN |
| --- | --- | --- |
| P exact start→payload | PASS | 6 shared +241 evidence; 0 |
| L exact start→final native | PASS | 15 Part A; 0 |
| P genuine complete base96416b6b5bd57094576e9aba0a42d682584ec479→payload | PASS | 89 shared +5937 evidence +6 indexes; 0 |
| L genuine complete basef09fd6e88edc5049b026b16b0158e7e188091d2d→native | PASS | 221 Part A +6 evidence; 0 |
| P controller0e2349ec→evidence-only payload | FAIL, retained | 237 evidence, no shared change; 0 |

The evidence-only shared-lane failure is the native rule requiring a genuine
shared change. It is not silently converted to PASS and no artificial source
anchor or foreign edit was added. The genuine whole author candidate contains
the actual new native source and passes. Strict ownership accepts all262
changed paths (247 P,15 L), each current raw file equals its committed blob.
The complete pre-build inventory preserves15324 of15331 baseline files;
the remaining seven are the specifically authorized existing native outputs.
No target, plan, predecessor, legacy review/QC, handoff or foreign proof changed.

Native Git whitespace results are retained exactly in scope JSON:

- P increment and evidence-only both exit2: actual raw native stdout includes
  CRLF that Git flags as trailing whitespace. Do not rewrite those hash-bound
  command transcripts to manufacture a clean whitespace result.
- P complete exits2 and also contains pre-existing historical whitespace.
- L increment and complete exit0.
- Separate source, authored work-order/result and controller/checker whitespace
  check exits0. This limited result is not a blanket P-whitespace PASS.

Both branches were fetched normally before publication. Governance freshness
passes against origin/main96416b6b5bd57094576e9aba0a42d682584ec479; same-owner,
same-task, expected-branch checks pass for both worktrees. URL-index check
reports current. Dashboard/roadmap state is root-owned and unchanged.

## Index tail and review routing

Commit this scope/publication payload next, then regenerate only the four
reports/github-agent-index-{platform,lessen}.{json,md} files in a separate
terminal commit. Every generation AND freshness invocation explicitly supplies
both FOURVECO_PLATFORM_ROOT/SOURCE_REF/SOURCE_BRANCH and
FOURVECO_LESSEN_ROOT/SOURCE_REF/SOURCE_BRANCH. The lesson ref is exactly
994657c8901729fcb138493be4e8c2e9590081bd; the platform ref is the actual
pre-index publication commit. Normal push, clean claims, fresh paired indexes
and HEAD=tracking=ls-remote must be verified before the final root handoff.
The final message provides those actual resulting heads, not invented hashes.

Import the author history after the stated bases through this scope/publication
commit; exclude the terminal four-index-only tail when root recomposes its own
paired index. Lesson history preserves r1 and pagination-refined r2 before the
final native successor. All original native PENDING and failed diagnostic
records remain inspectable, along with separate exhaustive author observations.

No PR or merge is created: root owns combined integration. No current hosted
platform-ci/validate-platform or full repository suite is claimed; root must
obtain actual aggregate commit-specific CI, and this packet does not waive it.
Next gate: independent complete paragraph review → a distinct specialist QC
→ root acceptance/handoff. The author stops here; classroom timing stays
UNOBSERVED and the isolated failed-cleanup temporary fixture is disclosed in
the correction log and result report.

## Native index diagnostic after the scope commit

The first exact-platform-ref index attempt failed before writing any index:
the existing child-process default stdout buffer cannot contain this branch's
complete ls-tree output. The commit exists. A read-only large-buffer diagnostic
records the actual exception, output byte length/hash and file count in the
separate index-diagnostic JSON; no shared tool or buffer setting is patched.
The freshness check immediately afterwards accurately reported stale old
indexes, not success. Use the existing explicit platform SOURCE_REF=HEAD path
after committing these diagnostic records; its source_commit still binds the
actual immutable pre-index HEAD. Verify the full resulting file set against
actual tracked Git paths. Lesson SOURCE_REF remains the exact commit above;
all six paired environment values remain explicit on every invocation.

The actual fallback inventory comparison then found exactly one extra path:
the worktree .git pointer (13681 indexed versus13680 tracked, no missing paths).
That attempt is rejected, with index hashes and the complete comparison in
index-fallback.json. Final publication instead uses index-runner.js: only the
read-only Git child-process stdout buffer is increased to64MiB in a task-owned
process, which executes the unchanged native index script through Node's normal
main-module loader. It verifies the shared script's entire bytes against the
original task base first. No shared tool, Git policy or content guard is changed.
Both final refs are exact committed hashes, not HEAD fallback. Verify index
file sets against actual Git before committing the four-index-only tail.
