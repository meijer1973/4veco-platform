# Exact accepted-successor binding plan — no source changes yet

Accountable integrator: codex-root. Date:2026-09-05. This is a bounded execution
detail of the approved production continuation, not a new authority decision.
Quality floor: every final generator must reproduce its own reviewed student
payload from actual accepted current prerequisite files, while rejecting any
other prerequisite bytes before subprocesses or output writes.

## Required sequence and owners

1. Finish current independent paragraph and distinct specialist gates for211,
   212,213,221,222,223. Root adopts exact published decisions, verifies current
   source/output/proof bindings and records accepted handoffs only when justified.
2. Once211's corrected package is accepted, the distinct212 builder may replace
   its two exact accepted-input constants with that reviewed successor's actual
   canonical-LF hashes. It may not change212 student text, assets or layout.
3. Once211 and212 corrected handoffs are accepted, the distinct213 builder may
   replace its four incoming handoff/review/QC pins and212 paragraph-source pin.
   The213 plan/chapter/target pins and student payload remain unchanged.
4. Once221 R8 is accepted and handed off, the distinct223 builder may replace
   its four incoming221 handoff/review/QC/source pins. The223 student payload
   remains unchanged. This can run independently of steps2–3.
5. A non-author integration-delta reviewer judges each exact successor change,
   rejection coverage, complete regeneration and unchanged student payload.
   Root refreshes current lineage/quality/handoff bindings only after this gate;
   historical reviews, manifests and old generator checkpoints stay immutable.

These are dependency-scoped steps, not a requirement to finish all six reviews
before starting any successor. Execute each eligible branch as soon as its
own named prerequisite records are accepted. Unrelated pending reviews do not
create a new hold.

No action in this plan is permission to manufacture an acceptance record,
skip pending QC, loosen a guard or use an arbitrary allowed-hash list. It does
not delay unaffected paragraph planning; later production still requires its
own prerequisite teaching, reviewed plan and action-specific authority checks.

## Concrete source contract inspected by root

| Dependent generator | Exact incoming fields | Hash semantics |
|---|---|---|
|b2_212.py|PRIOR_REVIEW_HASH and PRIOR_QUALITY_HASH for211|UTF-8-sig decoded, CRLF/CR normalized to LF|
|b2_213.py|211 handoff;212 handoff/review/QC in prerequisite_pins(); separate212 paragraph MD check|Four canonical-LF hashes; paragraph MD raw SHA256|
|b2_223.py|PRIOR_PINS:221 handoff/review/QC/paragraph MD|All four canonical-LF via lf_hash, including MD|

No hard incoming acceptance pins exist in221/222 generators. Their historical
teaching lineage still needs current root documentation; no invented source
guard changes are needed there. A later handoff is not itself a new student
lesson revision. Historical test/source commits stay immutable. Original
economic test bodies remain intact; only the expressly bounded whole-file
expected-successor evolution below is allowed in newer212 metadata tests.

## S-1 correction: exact whole-file expectations across accepted successors

Independent review of this plan at788145fbdbb8731c8dd7d836a07cf259932780e2
required S-1 (report successor-binding-independent-review.md, raw SHA256
b3a3f161b7a4ea23817af6f071d4d807d3bd7c4369b8bc4b910be9c2a420c46a).
Root confirmed actual212/test_metadata.py from original R6 commit
89a8fc34f7c017b10af86d6b058bf6ba21328367 equals the current root file.
Both named whole-file guards use immutable historical source baseline
798cacfeeb40e4e0ba54d26f2b040cbdeec327a9. Keeping the expectation string
unchanged would reject the authorized bonus/pin transitions; adding new tests
alone would not resolve it. This section changes the plan, not any source pin.

Each phase must inventory actual full-file guards on its exact published
starting pair. Preserve the original ten212/test_source.py test bodies/AST.
Retain all five metadata protections, nine alt-insertion count, full captions,
old long-alt negative fixtures, five contextual titles and title negatives.
Expected bytes must derive from immutable published source plus explicit
literal transformations, never from whatever the live file currently contains.

1. The separate212 bonus builder may evolve only
   test_nine_exact_native_insertions_and_unchanged_full_sources: in answers.md
   expectation, add the exact reviewed criteria label/list at its unique
   bonus→Herhaling boundary, in addition to the nine original alt insertions.
   Every other source byte remains compared exactly. That phase has its own
   work order, negative missing/extra/misplaced/altered-model/unrelated-source
   fixtures and distinct review; it is not part of a pin-only assignment.
2. The later212 successor builder starts from that accepted bonus/test baseline.
   It preserves the complete reviewed source expectation, including the exact
   bonus block, and changes no pupil source. In the named
   test_unchanged_generator_outside_title_loop expected generator only, it may
   add exactly two literal substitutions after the existing five-title-loop
   transformation. Match each full old assignment once, replace once:
   PRIOR_REVIEW_HASH old92b4a9462caf8316274fb58f8beef5c850147c44e6bf80b9a28fad442d9dbe96
   and PRIOR_QUALITY_HASH old0dddb6e9d8f3a8da0e0f31e67dafabf53b99feb6ad86ce72039480dd7e12ea18.
   New values must be actual canonical-LF hashes of the separately accepted
   current211-review.md/211-quality-ref.yaml at an exact published predecessor
   commit (full names2.1.1-review.md/2.1.1-quality-ref.yaml). Record old→new
   full lesson paths, SHA values, predecessor reviewer/root acceptance and
   commit identities before implementation. No speculative new hash is allowed.
3. Continue exact whole-generator equality after those two substitutions.
   No arbitrary accepted-hash list, skipped test, broad normalization, filtered
   source section, title/caption waiver or self-derived expectation is allowed.
   Full original regression history stays inspectable. Any unrelated generator
   or source edit must fail the exact comparison. The other metadata test bodies
   and all original economic tests remain unchanged.

Independent non-author delta review must check these precisely bounded expected
transformations, not merely test counts. New accepted inputs must build; each
altered/unknown required input must fail before subprocesses/output writes;
unrelated source/generator mutations must fail; every own student artifact/page/
archive member must remain byte-identical to the accepted pin-phase baseline.
This exception is restricted to the two named expectation transitions. If the
actual baseline introduces another conflicting guard, stop and obtain a bounded
reviewed plan correction, rather than widening this exception silently.

## Pre-change evidence and actual implementation checks

Each builder uses a new claimed paired worktree at a published root baseline,
reads the current accepted predecessor decisions itself, and records exact
old→new paths, hashes, published commits, reviewer and root handoff. Read the
original source and test contracts before editing; no speculative hash values.
State the complete expected output inventory before generation.

Prove exact source diff limited to specified hash literals, bounded new
regressions and the explicitly reviewed S-1 expectation substitutions above.
Prior economic test bodies retain their AST/meaning; full-file protections
retain exact equality around only the named authorized transformations. Add tests
that an altered required review/QC/handoff/source fails before output/process
work, using isolated fixtures or mocks; never corrupt a real lesson file.
Valid newly accepted inputs must permit the existing native build.

Run native full and print-only reproduction and verify all own source-derived
Markdown/HTML/PDF/SVG/PNG/ZIP bytes remain identical to the currently reviewed
payload, with matching page hashes and archive member bytes. Preserve exact
input→generation→review lineage in a fresh evidence record; do not overwrite
old PENDING manifests or claim an old source hash is still current. New proof
names use the unchanged next-unused rN guard where new captures are required.

Use explicit C:/Python314/python.exe.212/213 use inherited PATH;223 requires the
already evidenced MSYS-first native font environment and extended Windows
lesson path when needed. A different runtime causing drift is a failed attempt,
not grounds for restoring bytes manually or weakening equality checks.

Run normal Part A student-web/publisher-print profiles, scoped currentness and
durable frozen authority. Commit payload before actual-base scope checks; then
publish separate scope/evidence and generated-index tails. Independent delta
review receives the exact published pair and all failures/corrections.

Stop for unexpected pupil-byte change, unknown paths, missing accepted input,
review/QC contradiction, drift in targets/plan/semantics, altered history or
an ownership collision. Root owns integration, aggregate review and final CI.
Classroom observations, PartB commissioning and future payload merge authority
remain outside this technical succession step.
