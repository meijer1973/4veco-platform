# Sprint R0.1: Baseline

## Plan reference

`docs/sprints/R0.1-plan.md`

## Current Repository State

- `docs/sprints/` did not exist before this sprint.
- `reports/sprints/` did not exist before this sprint.
- `references/data/sprints/` did not exist before this sprint.
- `build-scripts/sprints/` did not exist before this sprint.
- `build-scripts/references/` contains many `unit-*` and `term-*` scripts, although its README still describes the suite as not yet built. Treat that README sentence as stale until separately corrected.
- `references/machine/` and `references/external/` are protected surfaces and are not changed in R0.1.

## Compatibility Scores For Handoff Roadmap Headings

Scale: 3 = compatible now, 2 = compatible with small path/status correction, 1 = directionally useful but requires design decision, 0 = incompatible or unsafe as written.

| Heading | Score | Notes |
|---|---:|---|
| 0. Executive summary | 3 | Matches the repository direction: governed references before RAG/adaptive work. |
| 1. Current known source context | 2 | Mostly accurate, but `build-scripts/references/README.md` is stale about CLI implementation status. |
| 2. Non-negotiable operating rules | 2 | Protected-surface rules match the repo; proposed `references/data/` and `docs/sprints/` paths are new. |
| 3. Sprint lifecycle | 2 | Useful, but branch/commit/tag discipline should be applied by explicit user request per sprint. |
| 4. Sprint artifact templates | 3 | Compatible and safe. |
| 5. Target architecture | 1 | Directionally useful, but proposed renames like `units.json` vs current `micro-teaching-units.json` need a source-of-truth decision before execution. |
| 6. Core data objects | 1 | Useful future model; needs schemas and migration plan before implementation. |
| 7. Review-gate model | 2 | Compatible with existing QC prompt philosophy, but scripts/folders do not exist yet. |
| 8. Roadmap phases and sprints | 2 | R0.1 is safe to execute; later phases must be revalidated before implementation. |
| 9. Review gates summary | 1 | Needs actual gate validators and owner rules. |
| 10. Critical path | 2 | Correct ordering principle; details need repo-state checks per sprint. |
| 11. Immediate Codex instructions | 3 | R0.1 is safe and scoped to infrastructure. |
| 12. Final execution rule | 3 | Matches the repo's machine-reference and human pedagogical decision boundaries. |

## Suggestions Before Treating The Full Roadmap As Authoritative

- Resolve the sprint ID collision: `references/reference-team-roadmap.md` already uses `R0.1` for A-domain skilltree consistency.
- Update `build-scripts/references/README.md` to reflect which CLI scripts now exist and which are still contractual/incomplete.
- Decide whether future data paths should live under `references/data/` or under the existing `reports/` and `references/machine/` split.
- Keep RAG, diagnostics, teacher cockpit, and bounded AI phases as future architecture until reference source manifests and review gates exist.

