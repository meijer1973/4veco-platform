# Sprint R7.5: Baseline

## Plan reference

`docs/sprints/R7.5-plan.md`

## Baseline state

Branch: `refsys/R0.2-reference-baseline-inventory`

R7.4 is closed as `pass_with_conditions`.

Known merge-readiness concerns before execution:

- The branch was reported by lead-developer review as diverged from `main`.
- Both `reports/review-gates/GATE-R5-alignment-graph/` and `reports/review-gates/GATE-R5.3-alignment-graph/` exist.
- `references/final-codex-handoff-roadmap original.md` is present under the reference corpus and needs relocation or classification.
- `build-scripts/references/validate-all.js` is absent.
- `build-scripts/references/validate-core-schemas.js` is present.
- R3.2 mutation logs are present under `reports/review-gates/GATE-R2-empty-needs/`.

## Data integrity notes

No protected reference data changes are allowed in R7.5.

`references/machine/` and `references/external/` must remain read-only except for merge conflict resolution if the same protected files already differ between branch and main. Any protected machine changes must be validated against mutation logs and validators.
