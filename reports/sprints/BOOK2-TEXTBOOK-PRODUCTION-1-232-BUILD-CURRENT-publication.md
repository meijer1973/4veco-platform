# §232 current native source-author publication

Actor/task/branch: `paragraph_231_specialist_qc` /
`BOOK2-TEXTBOOK-PRODUCTION-1-232-BUILD-CURRENT` /
`agent/book2-232-build-current-20260906` in the paired
`C:/wt/book2-232-build-current-20260906/{4veco-platform,4veco-lessen}` worktrees.
Both same-owner/same-task claims checked; no override, foreign branch mutation or merge.

## Immutable payload and source binding

Final pupil/controller source: `e0b47cab498102cd990e66318f5111602c32a6b6`.
Complete source/native/evidence payload used by the actual scope checks:
P `02be6566ef43bf6c468c0a75a9f43c7ff3731e1d` /
L **`ea0dd35d4f919ac5b3aae533cd9f9022ffa62282`**.
The latter is the sole lesson commit, exactly41 new planned native files.
This publication and its actual scope/process/claim evidence are an additional
evidence-only platform commit, not a new pupil source revision.

Chronological OWN importable platform commits before this closure commit:

1. `3cd7b23425c5977adb17bb99cfc399ec0e21225d` operational plan.
2. `460ebbf6ff3230ab8e4e38839393c5fc4e1c0a13` initial eleven sources.
3. `9512c8c69cd099f32cd20eaa0f162586a03c42fa` measured ink clearance.
4. `d974d657f27da7a36febb024e1abd488c9d45c0f` LF technical Git fixtures/positive control.
5. `6cfdd4ac8c617e7f3b165b203ad48ec249e5b913` own reservation distinction.
6. `f23543ac24f7b4cf230e3fcd6454246eef866d34` quiet labels/exact heading whitespace.
7. `aee3d05641d2b76f776be9e996ee1dc4505130be` full nested/large proof history.
8. `e0b47cab498102cd990e66318f5111602c32a6b6` final table word/mask geometry correction.
9. `97cc22e144edf88204dabd1ab4bea88b57fbd5cc` complete author evidence/all proof histories.
10. `02be6566ef43bf6c468c0a75a9f43c7ff3731e1d` batched proof Git read/helper diagnostic.

Root should include this publication/scope closure commit as well and exclude the
later exact four-index-only terminal tail. Final response supplies those exact SHAs.
All are OWN commits; no foreign review/acceptance import is buried in this sequence.

| Closure file | Raw SHA256 |
| --- | --- |
| `...-result.md` | `a1b6b4a126fdb146c0d1e07635f8eb8bbd6106292973f7565e027bf35ede6824` |
| `...-personal-binding.json` | `0d75d838d53f70ef4b419224d3bf165d77d8bd162449f86cef6949875ee335eb` |
| `...-scope.json` | `eaa00dcfe284d4e48c4c67a20c6e2b9d844f1de1a0166346f3b83ffd0f4e602b` |

`...-result.md` is the complete source/math/PartA/teacher14/student12/visual10/
rendered-checklist/limits report. `...-personal-views.json` is the author's actual
112-view statement; the binding helper verifies coverage/raw bytes but performs no
visual review. All current native work is finished: full r1000003, thin r1000004,
gated direct r1000005 and actual checker --rebuild r1000006 agree in all41 native
bytes,42 raw/decoded pages and ZIP25/13/13. All112 current color/grayscale personal
page/figure views are complete. No remaining visible defect was observed by the author.
The original generated proof manifests remain immutable PENDING records.

## Actual scope and whitespace, not inferred PASS

`...-scope-batched-process.json` records the exact successful invocation of the
unchanged native lane classifier and complete committed/raw-byte own-scope audit.

| Actual comparison | Scope | Categories |
| --- | --- | --- |
| P initialbc49→payload02be | PASS |11 shared sources,615 evidence,0 UNKNOWN |
| L initialf666→payloadea0d | PASS |41 PartA files,0 UNKNOWN |
| P current main96416→payload02be | PASS |101 shared,6 indexes,7871 evidence,0 UNKNOWN |
| L mainf09fd6→payloadea0d | PASS |266 PartA,7 evidence,0 UNKNOWN |
| P sourcee0b→payload02be, evidence-only | **FAIL exit1** |614 evidence,0 native source anchors,0 UNKNOWN |

Evidence-only FAIL is genuine: shared lane requires a shared platform source change
and allows review evidence only with lane-owned changes. It is not relabeled PASS,
nor fixed with a fake anchor or classifier widening. Independent strict owned scope
checks **667 paths** across both repos against the exact11 sources, own sprint/proof
namespace and exact41 native files; all raw bytes equal the immutable commit blobs.

Actual `git diff --check`: L increment and whole PASS exit0. P increment/evidence-only
exit2 for the already recorded personal-views JSON's extra EOF blank line. P whole
exit2 also includes pre-existing immutable Windows command-log trailing whitespace
and probe diagnostics (complete stdout retained). No printed source whitespace defect
is hidden by this description. Original evidence bytes were deliberately preserved,
not rewritten to manufacture whitespace PASS.

The first scope helper invocation failed at its one large binary `git cat-file --batch`
capture (`status:null`, assertion preserved in `...-scope-process.json`). Its original
helper did not record the lower-level error, so a specific ENOBUFS diagnostic is not
claimed. The aggregate proof payload exceeded the intended single capture size;
bounded batches of16 reads using the same raw binary parser then passed. This was a
task-local evidence transport correction only, no source/native/authority edit.
A worktree-safety CLI call initially omitted its required `--check` mode; it failed
without effects. Correct actual claim checks are separately retained.

## Normal publication sequence

Both repos were normally fetched before closure. Platform governance currentness
matches origin/main `96416b6b5bd57094576e9aba0a42d682584ec479`, no differing governance
file. The final terminal sequence must and will be verified before the final response:
commit this evidence-only closure, refresh the four paired GitHub index files, verify
literal-NUL exact source inventories and native index freshness, commit exactly those
four files as a separate tail, normal push both unique branches, then assert clean
same-owner claims and HEAD=tracking=actual ls-remote in both repositories.
This paragraph is the sequence contract, not a fabricated already-run remote result.

Every index invocation supplies explicit platform+lesson roots, exact40-hex source
refs and actual branches. The task-local runner checks the entire shared index writer
against the initial committed bytes, changing only read-only Git transport to128MiB
and `core.quotepath=false`. An independent literal-NUL inventory check compares exact
sets/counts; no `.git` fallback, guessed HEAD or shared writer change. The root's own
branch-specific runner is not reused. No roadmap/dashboard/source authority changed;
only the four paired inventory files form the generated terminal tail.

## Boundary and next action

This is a complete authored candidate, not independent review, specialist QC,
root acceptance, accepted teaching handoff, student release or hosted-CI proof.
Both actual paragraph profiles remain FAIL solely for absent independent review and
QC files (2errors/0warnings). These required downstream gates remain PENDING and
`production_ready:false`; they must not be filled by this author. All timing remains
UNOBSERVED54/74/83/88. No PR was opened: root owns the combined integration lane.
No future PR merge or PartB authority is implied. Latest exact-head hosted CI pending.
Next: root assigns a DISTINCT paragraph reviewer using this exact published pair.
