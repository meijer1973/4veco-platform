# Sprint R3.2: Baseline

## Plan reference

`docs/sprints/R3.2-plan.md`

## Baseline summary

R3.2 starts after a bounded human mutation-review decision authorized specific R2.4 packet items for CLI-only mutation.

Baseline approved edge count: 15.

Baseline approved zero-needs classifications:

- `A01`
- `A02`
- `A03`
- `A04`
- `A05`
- `A38`

Baseline protected-reference state:

- `references/machine/micro-teaching-units.json` contains 192 units.
- Approved edge targets and prerequisites exist in the current catalog.
- The unit CLI did not previously expose `--dry-run`; R3.2 adds it for `unit-add-dep.js` and `unit-update.js` before executing mutations.

## Data integrity notes

R3.2 is allowed to change protected reference data only through CLI commands. No hand edits to `references/machine/` are allowed. `references/external/` remains protected and out of scope.

## Known risks

- Any approved edge could still fail validation because of cycles or current catalog drift.
- Existing generated reports may need regeneration after the machine registry changes.
- D04 and held items must remain out of scope even though they are nearby conceptually.
