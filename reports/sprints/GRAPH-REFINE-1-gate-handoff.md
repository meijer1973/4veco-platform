# Sprint GRAPH-REFINE-1: Gate Handoff

Generated: 2026-05-31

## Purpose

Record what later gates and sprints must know before graph/table output can be
used for target-equivalent exit-ticket evidence.

## Handoff Summary

`GRAPH-REFINE-1` finds that the current graph/table route is useful local
practice and remains the best reference pattern, but it is not yet
target-operation aligned for `1.1.3`.

Primary blocker:

```text
The current route contains price-as-horizontal/x wording, while the target
exercise requires price on the vertical axis and quantity on the horizontal
axis.
```

## Required Before Target-Equivalent Graph Use

Before `1.1.3` graph output can support target-equivalent exit-ticket proof,
the team must show:

1. target graph tasks use price vertical and quantity horizontal;
2. target table values are used from the ice-cream table;
3. interpolation at EUR 1.75 is practised or checked with a quantity answer
   around 350 ice creams;
4. 50 percent drop interval is identified as EUR 2.50 to EUR 3.00 using
   values 200 and 100;
5. answer-form coverage includes source/table evidence plus calculation or
   reasoning plus short explanation;
6. the shared task shell remains the interaction layer;
7. rendered output is inspected in light/dark and desktop/mobile states;
8. no advisory/practice route copy claims target-equivalent proof.

## Handoff To Future Sprints

| Future work | Handoff requirement |
|---|---|
| `GRAPH-REFINE-2` if started | Implement target-aligned graph route repairs and proof without target-equivalent claims |
| `CHECK-Q2-PLAN` | Use this coverage matrix when planning target-equivalent graph/table checkpoint composition |
| `L1.7B-Q2` | Do not rely on current GRAPH-UX-2 graph route as proof until the axis and target-chain gaps are repaired |
| `GATE-L1.7B-Q2` | Inspect rendered target-equivalent output and confirm complete operation-chain coverage before completion language |
| `Scale Gate 1` | Treat graph route as practice evidence only until target-equivalent proof closes or is explicitly waived |

## Prohibited Inferences

Do not infer:

- completing the current graph route proves the target exercise;
- the current axis-convention task satisfies `1.1.3`;
- source/table use alone is a complete answer form;
- point placement with price as x is equivalent to the target P/Q graph;
- checkpoint fixtures are published target-equivalent exit tickets.

## Product Boundary

No target-equivalent completion language is authorized. The following remains
held:

- `Je hebt laten zien dat je de eindopgave van deze paragraaf aankunt.`
- `Je kunt nu door naar de eindopgave.` as proof/completion language;
- diagnostics, mastery, sequencing, summative status, student-facing AI, PV,
  Scale Gate 1, and product-use claims.

Allowed current meaning:

```text
The graph route is useful local practice and a strong reference pattern, but
needs target-chain repair before proof use.
```
