# Initial root212 baseline read diagnostic

The first actual baseline command failed before writing baseline.json or any
native proof. Imported270-file byte custody and specialist proof/observation
checks completed; whole-lesson raw inventory encountered Windows' ordinary-path
length limit while reading the existing unrelated223 answers.html. Git's UTF-8
path is intact; no missing pupil file or rendering defect was established.
The actual traceback/exit1 is retained in the root native command log.

The root helper now uses the explicit Windows extended prefix for raw file
reads only. It neither changes stored repository-relative paths nor replaces
content/encoding. The evidence folder contains only this diagnostic; baseline
retry permits exactly that state and still exclusively creates baseline.json.
No output restore, deletion, source change, accepted-byte exception or reuse of
a native proof namespace is authorized by this instrumentation correction.
