# Sample Y

The three PDFs and top-level Markdown records are provided directly for review.
[sample-y-artifacts.zip](sample-y-artifacts.zip) contains the complete paragraph sources, HTML,
PDFs, build script, SVG/PNG pairs, final-page proof, logs and independent review
history with their original relative paths. Evidence paths inside the review
records refer to that extracted archive layout. Dependency installations and
Python caches are omitted. PDFs and all archived originals are byte-exact.
The readable Markdown copies beside the archive use LF line endings, trimmed
trailing whitespace and one final newline for Git hygiene. Both original and
copy hashes are in the manifest; audit reviewer snapshots against the originals
inside the archive. No original source/output file was edited.

The artifact manifest lists every archived file and its SHA-256. These are
isolated comparison materials; full quality-reference closure and student-use
authorization are absent. See the parent comparison report for the limits.
