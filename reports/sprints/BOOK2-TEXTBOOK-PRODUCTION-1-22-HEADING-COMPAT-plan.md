# Chapter assembly: exact reviewed heading separate from legacy path

2026-09-06, codex-root, sole integrator, BOOK2-TEXTBOOK-PRODUCTION-1 / #229.
Operational order before implementation. Clean published root base
P9efd181d56fbebca33137c3644eadf184ebe9f3d /
L3bc9e81828dcc57932dc871825aaa4cf4a975fe1; dedicated paired worktrees
C:/wt/book2-part-a-production-20260905/{4veco-platform,4veco-lessen}, branch
codex/book2-part-a-production-20260905. Normal fetch/current governance and
same-owner clean platform claim pass. No foreign task or source is edited.

## Problem, quality floor and bounded correction

The actual accepted223 source H1 says `Inkomenselasticiteit en kruislingse
elasticiteit`; its preserved legacy folder/filename says `kruiselingse`.
The current shared chapter prepare function rejects that valid source because
it equates H1 with the filesystem stem. This was found during root's complete
source inspection after the candidate-only publication, before independent
candidate review or any production release. Keep the original candidate and
all prior checks unchanged; they prove input custody, not working assembly.

Quality floor: exact complete reviewed source preserved once in the correct
edition; independent source identity, whole-file hash and destination/path
validation before effects. No title rewriting, folder migration, monkeypatch,
runtime source replacement, permissive prefix bypass or aggregate content edit.

Change ONLY build-scripts/content/book-2/chapter_pipeline.py and its existing
test_chapter_pipeline.py. Add an optional explicit `source_headings` map with
exact keys `student` and `answers`. Each value must be a nonempty one-line
Markdown H1 naming the exact paragraph ID. With this map supplied, require
the exact first nonblank source line, independently of the unchanged filesystem
stem; both full source hashes still pass. Without the map preserve existing
legacy behavior. The map is a caller-owned reviewed input, not an authorization.
Do not add any content, styles, output path, asset or proof namespace behavior.

## Evidence and review procedure

Main reread BUILD-CHAPTER, complete chapter builder/assembler skills, complete
chapter_pipeline and its test module, actual223 H1s, previous front/consistency
review and C22 assembly plan. Earlier continuous-task PDF/strategy/didactic/
precision/terminology instructions remain applicable. User-specific 12pt/full
printed guidance override generic skill examples; neither changes here.

1. Commit this order and an owned read-only probe. Build the exact eight-source
  C22 spec from the published immutable candidate (all138 prior inputs must
  still match at baseline). Preserve the actual old-source rejection for223.
  The probe never calls native build/render or writes generated lesson files.
2. Implement optional exact heading-map validation and tests. Prove legacy six
  tests retained, both editions preserve byte-derived content with corrected
  display title, wrong/missing/extra/multiline/wrong-paragraph headings reject,
  unchanged whole-source hashes reject drift even with forged offered heading,
  and no native build worker or output mkdir/write is reached on failure.
3. Run all original plus new unit methods. Run actual full eight-source C22
  prepare with exact map, retaining full eight source hashes/thirty assets,
  complete student/answer source once and no duplicate standalone exercises.
  This produces strings in memory only, not native PDFs or visual acceptance.
4. Capture source-commit/raw/stdout/stderr and compact whole no-filter Git
  preservation excluding only these two sources and terminal indexes. All
  lesson files, targets, canonical review/QC/handoffs,29 PDFs and manifest stay
  exact. Current chapter_planning/production,durable,bundle/governance checks.
5. Publish this source with a NEW R2 immutable complete C22 input candidate,
  exact current two source bindings and exact eight heading map. Preserve the
  published R1 checker/manifest/evidence byte-for-byte; do not amend old hashes.
  R2 continuity must establish all other136 current input bytes unchanged and
  retain original source history. Repeat whole candidate negative cases.
6. A distinct agent reviews the complete changed source, actual input contract,
  heading/path mapping, teaching/acceptance chain and rejection boundary. Only
  after PASS may root issue a separate immutable chapter production release.

Scope permits own22-HEADING-COMPAT-* and new22-INPUT-R2-* evidence/controllers,
the two declared shared source files and the four terminal indexes. No L or
output-manifest writes. Native narrow/whole scopes retain honest empty-L FAIL,
zero UNKNOWN, exact whitespace; normal publication/map refresh and clean remote
verification close this phase. No current full CI, independent PASS or merge
authority is invented. Native chapter build/parity/all-page personal review,
root acceptance and combined41PDF/final CI/lead/readiness remain next gates.
