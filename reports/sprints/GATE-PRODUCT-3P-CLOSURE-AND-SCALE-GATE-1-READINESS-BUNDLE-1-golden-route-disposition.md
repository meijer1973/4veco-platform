# Golden-Route Disposition

Date: 2026-06-19

Verdict: `HOLD_FOR_GOLDEN_ROUTE_REPAIR`

## Current Route Architecture

The current first-three proof confirms the following route shells:

| Paragraph | Exit ticket shell | Short-check shell | Disposition |
|---|---|---|---|
| `1.1.1` | `legacy_task_shell` | `legacy_task_shell` | not scale-ready |
| `1.1.2` | `golden_exercise_workbench` | `golden_exercise_workbench` | acceptable Golden transfer/advisory proof |
| `1.1.3` | `golden_exercise_workbench` | `legacy_task_shell` | exit ticket acceptable; advisory route not scale-ready |

## Narrow Gate Decision

The mixed shell state does not undo `GATE-PRODUCT-3P` current-main closure. The
gate claim is bounded: first-three rendered path proof, route/link coverage,
neutral authority copy, completion language held, and no downstream authority.

## Scale Gate Decision

The mixed shell state blocks `Scale Gate 1`. The Golden Workbench roadmap says
Golden Workbench is route architecture, not a CSS patch, and that broad
rollout has not proved `1.1.1`, graph-based advisory short checks, all short
checks, or Scale Gate 1.

Scale Gate 1 should not rely on a first-three set where:

- `1.1.1` still uses the legacy shell for both check surfaces;
- `1.1.3-korte-check` still uses the legacy shell;
- the Golden advisory graph variant has not been planned and rendered;
- Start pages still contain mastery/closure phrasing;
- no refreshed post-repair first-three proof exists after those migrations.

## Required Repair Sequence

1. Run `GOLDEN-ROUTE-111-MIGRATION-AND-START-COPY-REPAIR-BUNDLE-1` as the next
   substantial bundle. Treat `EXIT-SHORT-WORKBENCH-111-PLAN-1` as an internal
   phase, not a human-review stop.
2. Within that bundle, plan `1.1.1` route migration without changing
   target-readiness flags.
3. Render `1.1.1` exit ticket through Golden Workbench or record a hard blocker
   with exact repair plan.
4. Render `1.1.1` short check through Golden advisory Workbench or record a hard
   blocker with exact repair plan.
5. Repair first-three Start-route mastery/closure copy.
6. Then run `GOLDEN-GRAPH-ADVISORY-113-BUNDLE-1`.
7. Then run `A96-CALCULATION-WORKBENCH-REFINE-1`.
8. Refresh first-three rendered proof and prepare the actual Scale Gate 1
   review in `FIRST-THREE-POST-REPAIR-PROOF-AND-SCALE-GATE-1-REVIEW-1`.

## Forbidden During Repair

- Do not authorize completion language.
- Do not authorize product-route adoption.
- Do not authorize diagnostics, mastery/sequencing, PV, summative use,
  Scale Gate 1, broad product use, or student/product use.
- Do not mutate target-readiness flags unless a strict source/generated
  mismatch is discovered and separately justified.
- Do not hand-edit generated lesson output.
