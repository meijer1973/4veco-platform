# Sprint L1.7B-Q2: Baseline

## Plan reference

Plan: `reports/sprints/L1.7B-Q2-plan.md`

## Current state

`CHECK-Q2-PLAN` closed as planning/preparation only and did not authorize
implementation. It says later Q2 work must select one paragraph and resolve or
scope its blockers before implementation.

The selected first candidate is `1.1.2 Percentages en indexcijfers`.

Current source/output facts:

- `source-data/book-1/exit-ticket/1.1.1.json` exists and is an advisory
  `Korte check` with `targetReadinessEvidence: false`.
- `source-data/book-1/exit-ticket/1.1.2.json` is absent at planning start.
- `source-data/book-1/exit-ticket/1.1.3.json` is absent at planning start.
- The target-exercise registry has `1.1.2` required skills `A38`, `A39`, and
  `D31`.
- Current math practice supports A38/A39 local practice but does not by itself
  prove D31 target-equivalent coverage.
- Current task-shell `calculation_work_capture`, `short_constructed_response`,
  and `structured_reasoning` families are self-check families; implementation
  must not treat generic self-check as proof without a reviewed rubric or
  deterministic proof route.

## Data integrity notes

No protected reference data is authorized for mutation in this planning step.
`references/machine/`, `references/external/`, and
`references/authored/course-target-exercises.json` are read-only.

No source exit-ticket data or generated Book 1 output is authorized for
mutation until this plan passes planning review and the implementation phase
starts under the recorded stop conditions.

The existing untracked `knowledge/exit-ticket-game-1.1.1.zip` remains unrelated
and excluded from this sprint.
