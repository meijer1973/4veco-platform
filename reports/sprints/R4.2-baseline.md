# Sprint R4.2: Baseline

## Plan reference

`docs/sprints/R4.2-plan.md`

## Baseline summary

R4.2 starts after R4.1 completed unit-term slug migration.

Baseline observations:

- Exam-question records: 349.
- Exam papers represented: 12.
- Questions with empty `required_skills`: 27.
- Questions with empty `exam_codes`: 19.
- Questions with missing `question_type`: 0.
- Invalid current `required_skills` references: 0.
- Invalid current `exam_codes` references: 0.

## Data integrity notes

This is a non-mutating sprint. `references/external/` is read-only because exam questions are extracted external authority. `references/machine/` is also read-only because this sprint produces a patch queue, not unit or term mutations.

## Known risks

- Existing audit files may contain unresolved skill slugs rather than current unit IDs.
- Syllabus codes may be useful coverage metadata, but they are not sufficient evidence to mint units.
- The sprint must not become an endless pedagogical re-audit of every question; it should surface bounded gaps and route them to later review.
