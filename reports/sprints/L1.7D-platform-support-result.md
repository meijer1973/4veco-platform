# L1.7D Platform Support Result

Date: 2026-05-24
Status: COMPLETE AS PLATFORM SUPPORT; LESSON L1.7D CLOSED PASS WITH FLAGS

## Purpose

Support lesson sprint `L1.7D Paragraph Landing Page Information Architecture
Cleanup` by moving paragraph landing pages from a peer card/file layout toward
a controlled learning route.

## Changes

- Updated `build-scripts/platform/build-landing-page.js` so paragraph landing
  pages render route layers:
  - `Start`
  - `Leer`
  - `Oefen`
  - optional `Check`
  - `Verdiep`
- `Check` is hidden unless an exit-ticket/checkpoint surface exists.
- `Oefen` now presents guided support plus scoped practice routes.
- `Rekenen` routes to the generated `stappenplan` surface when it exists.
- The full `wiskundevaardigheden` skill map is demoted to a collapsed advanced
  source item when a scoped calculation/procedure route exists.
- Summary, news, presentation, textbook, and source/download surfaces are
  demoted to collapsed `Verdiep` groups.
- Student-facing Word exercise rows remain hidden from paragraph landings.
- `scripts/qa-student-web-pages.js` now validates paragraph route layers
  instead of assuming every resource-grid page must expose a graph route.
- `scripts/tests/build-landing-page.test.js` covers the new route IA and keeps
  legacy Word rows out of generated student landing pages.

## Validation

- `node --check build-scripts\platform\build-landing-page.js`: PASS
- `npx.cmd jest --runInBand --testPathPatterns "scripts/tests/build-landing-page.test.js"`:
  PASS
- `node scripts\deploy.js "..\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod"`:
  PASS, 460 references checked and 221 data tests passed
- Complete student-web validation for `1.1.1`, `1.1.2`, and `1.1.3`: PASS
- `node scripts\check-book.js --paragraph-mode part-a --paragraph-profile publisher-print
  "..\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod"`: PASS, 26/26
- `node scripts\validate-procedure-contracts.js --book-root
  "..\4veco-lessen\Boek 1 - Grondslagen, vraag en aanbod"`: PASS, 341 checks
- `node scripts\check-course-target-exercises-v5.js`: PASS, 54 records,
  counts 12/12/14/16
- Representative landing screenshot QA for `1.1.1` and `1.1.3`: PASS,
  desktop/mobile light/dark evidence
- `npm.cmd test`: PASS, 543 passed / 8 skipped

## Boundary

This support change does not authorize broad lesson scaling, adaptive
diagnostics, mastery decisions, automatic sequencing, grading/summative use,
student-facing AI, PV projection, PV machine promotion, target-exercise
promotion, unit minting, protected reference mutation, or product-use claims.

## Lesson Review Outcome

Lesson `L1.7D` closed as `PASS WITH FLAGS` after human review on
2026-05-24. No product blocker remained for the controlled pilot landing pages.

Carried flags:

- `Rekenen / stappenplan` remains a procedure route, not a full numeric
  calculation engine.
- `Check` remains hidden until a reviewed non-summative exit-ticket surface
  exists.
- Screenshot evidence is representative rather than exhaustive.
- `1.1.4 Gemengde opgaven` needs a dedicated consolidation landing pattern
  before broad scaling.
- Minor old route-variable/comment remnants in the generator can be cleaned up
  later; they are not student-facing.
