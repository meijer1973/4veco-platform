# §224 QC current operational runtime note

The initial snapshot used whole controller commit ae503563653b2ec948557241fccdf0974a9c994a
and bound nine existing source/shared/controller files. It captured 16,836 prior
raw files and all 34 actual plus committed release inputs before any native
attempt. Its successful immutable baseline is preserved as written.

Before the first reservation, code inspection found that matching `224` against
an entire worktree absolute path would include unrelated paragraph revisions in
this task's own `224`-named worktree. The scanner now matches paths relative to
each reports root (still recursively including all standard/nested proof,
reservation and failed-attempt names). No reservation or build had run yet.
The new committed whole guard also includes the newly authored independent
checks helper, for ten bound files. Actual native calls pin this successor
helper commit externally in the complete argv, not the initial snapshot commit.

Read-only lookup diagnostics in the tool transcript include wrong guessed
reference/test/chapter-plan names, a no-result guessed root remote branch,
PowerShell `rg` wildcard syntax, and transport-truncated reads subsequently
completed in bounded chunks. These are not successful file reads or content
defects. No original log was rewritten, no fallback inventory or global PATH
change was used, and the denied historical author cleanup fixture was untouched.

Canonical root/chapter plans retain historical drafting/pending prose. Actual
hold metadata, published approved plan release, predecessor QC/handoff and
root adoption distinguish later accepted authority; the historical prose is
not treated as current production denial or as a new owner grant.

The first independently calculated probe record has correct values but four
case labels numbered the cross-elasticity and function items one target item
too high. Direct rereading of the complete frozen record confirmed T4 combines
Ei/Ek, T5 is the function, and T6 is the advice. The owned helper labels are
corrected before native work and a new r2 probe record is written, leaving the
original successful numerical/failure-entry evidence unchanged. This is an
evidence-label correction, not pupil or target repair. Reservation r10 was
already exclusively captured after scanning 373 worktrees (prior maximum9).
