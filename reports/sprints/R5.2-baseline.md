# Sprint R5.2: Baseline

## Plan reference

`docs/sprints/R5.2-plan.md`

## Baseline summary

R5.2 starts after R5.1 completed the evidence-anchor layer.

Baseline observations:

- `references/data/evidence-anchors.json` exists.
- Evidence-anchor validator passes with 15 anchors and 13 claims.
- `references/data/alignment-graph.json` does not exist yet.
- `build-scripts/references/build-alignment-graph.js` does not exist yet.
- `build-scripts/references/check-alignment-graph.js` does not exist yet.
- `references/schemas/alignment-edge.schema.json` exists but is minimal.

## Data integrity notes

This is a non-mutating sprint for protected references. `references/machine/` and `references/external/` remain read-only. R5.2 creates governance data, tooling, and generated reports.

## Known risks

- The graph can look authoritative unless every edge carries review status.
- Generated-report evidence must remain secondary.
- R5.2 must not add retrieval or diagnostic behavior before R5.3 review.
