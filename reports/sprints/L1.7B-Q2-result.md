# Sprint L1.7B-Q2: Result

Generated: 2026-06-01

Status: completed implementation candidate; PASS WITH FLAGS.

## Plan reference

- Plan: `reports/sprints/L1.7B-Q2-plan.md`
- Baseline: `reports/sprints/L1.7B-Q2-baseline.md`
- Plan metadata: `references/data/sprints/L1.7B-Q2.plan.json`
- Result metadata: `references/data/sprints/L1.7B-Q2.result.json`

## Summary

L1.7B-Q2 implemented the first target-equivalent exit-ticket candidate for
`1.1.2 Percentages en indexcijfers`.

The sprint selected `1.1.2` because it has no existing advisory short-check
source to preserve, the target exercise has a compact reviewed operation chain,
and the shared task shell can support calculation work, final answers,
percentage/index notation, and short constructed response.

Implemented student-facing candidate:

- added source-controlled `source-data/book-1/exit-ticket/1.1.2.json`;
- generated the Book 1 `1.1.2` exit-ticket page through the platform deploy
  pipeline only;
- changed the generated landing card from advisory `Korte check` language to
  `Exit ticket` for this target-equivalent candidate;
- kept the existing `1.1.1` `Korte check` advisory-only with
  `targetReadinessEvidence: false`;
- added deterministic task-shell matching for reviewed calculation work and
  final answers;
- added stricter D31 text-criteria matching with rejected contradictory
  phrases;
- kept `targetEquivalent.gateApproved: false` and
  `completionLanguageEligible: false`;
- captured desktop, mobile/narrow, dark-mode, and completion-state screenshot
  evidence.

Lead review round 1 returned REVISE because a correct final answer could pass
with bogus calculation work and contradictory D31 wording could pass by
matching loose substrings. The correction added required work-text groups,
reject-text criteria, adversarial tests, and a stricter implementation checker.
Lead review round 2 returned PASS WITH FLAGS.

This sprint does not authorize target-equivalent completion language,
diagnostics, adaptive routing, mastery, sequencing, student-facing AI,
summative use, PV projection, PV machine promotion, Scale Gate 1, or
student/product use. `GATE-L1.7B-Q2` must decide whether the implemented
candidate is sufficient for local paragraph-completion copy.

## Acceptance test results

| Command | Status |
|---|---|
| `node build-scripts/sprints/check-sprint-plan.js reports/sprints/L1.7B-Q2-plan.md` | passed |
| `node build-scripts/sprints/check-sprint-bundle.js L1.7B-Q2` | passed |
| `node build-scripts/sprints/check-l1-7b-q2-implementation.js` | passed |
| `node node_modules/jest/bin/jest.js --testPathPatterns "engines/tests/(task-shell|exit-ticket).*\\.test\\.js"` | passed |
| `node scripts/deploy.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"` | passed |
| `npm.cmd run check:book -- "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"` | passed |
| `npm.cmd run check:platform` | passed |
| `npm.cmd run check:scope-language` | passed |
| `node build-scripts/reports/validate-report-json.js` | passed |
| `node build-scripts/references/check-roadmap-version-index.js` | passed |
| `node build-scripts/sprints/check-sprint-result.js reports/sprints/L1.7B-Q2-result.md` | passed |
| `node build-scripts/sprints/check-sprint-bundle.js L1.7B-Q2 --complete` | passed |
| `node build-scripts/sprints/emit-url-index.js --check` | passed |
| `git diff --check` | passed |
| `git -C ../4veco-lessen diff --check` | passed |

## Changed files

- Added the `1.1.2` exit-ticket source data.
- Added deterministic calculation-work and text-criteria matching paths to the
  shared task-shell engine.
- Updated exit-ticket engine/UI behavior so target-equivalent attempts remain
  gate-held and completion copy remains local/non-claiming.
- Updated landing and exit-ticket shell generation so a target-equivalent
  candidate renders as `Exit ticket` rather than advisory `Korte check`.
- Added focused Jest coverage for proof flags, correct paths, bogus work, and
  contradictory D31 wording.
- Added the `L1.7B-Q2` implementation checker, live-output evidence,
  screenshot manifest, screenshots, lead-review cycle, result, diff summary,
  and result metadata.
- Generated Book 1 output through `node scripts/deploy.js` only.
- Updated platform and lesson roadmap rows and generated repository indexes
  during closure.

## Data integrity notes

No protected reference data changed. This sprint did not edit:

- `references/machine/`;
- `references/external/`;
- `references/authored/course-target-exercises.json`;
- `references/data/exam-ingestion/answer-skill-candidates.json`.

No target-exercise `question_type`, `answer_form`, operation-chain, or mapping
fields were written. No candidate storage was created or written. No machine
reference mutation, external-source mutation, unit minting, unit update, unit
split, unit deprecation, PV projection, or PV machine promotion occurred.

The generated Book 1 output changed only through the platform deploy/generator
route. The existing untracked `knowledge/exit-ticket-game-1.1.1.zip` remains
unrelated and excluded from this sprint.

## Lead Review

Lead review was performed by reviewer agent `Sartre`
(`019e81a8-7cca-7911-b673-a0d53c2eb42f`).

Round 1 returned REVISE and found two proof-quality blockers:

- calculation work could pass with bogus work plus the correct final answer;
- D31 could pass with contradictory wording.

Corrections added required work-text matching, rejected contradictory text,
adversarial tests, refreshed generated output, and refreshed evidence.

Round 2 returned PASS WITH FLAGS. The carried flag is that the matcher is a
deterministic text-group matcher, not symbolic math parsing or semantic
language understanding. `GATE-L1.7B-Q2` must explicitly review whether the
accepted work/D31 criteria are sufficient before local target-equivalent
completion language can be approved.

## Open follow-ups

| Follow-up | Owner |
|---|---|
| Prepare `GATE-L1.7B-Q2` human-review packet with remote evidence, screenshots, answer model, implementation diff, lead-review flag, and exact completion-language question. | next gate-preparation sprint |
| Keep `targetEquivalent.gateApproved` and `completionLanguageEligible` false until `GATE-L1.7B-Q2` closes. | all checkpoint work |
| Do not expose paragraph-completion copy, diagnostics, adaptive routing, mastery, sequencing, summative use, PV, Scale Gate 1, or product use from this sprint. | all downstream work |
| Consider symbolic/rubric improvements only if `GATE-L1.7B-Q2` finds deterministic criteria insufficient. | future implementation sprint if authorized |

## Rollback instructions

If this sprint needs rollback before merge, revert only the L1.7B-Q2 source,
engine, test, generator, generated Book 1 output, evidence, roadmap, result,
and index changes made by this sprint.

Do not revert unrelated user work, prior sprint records, protected references,
target-exercise records, candidate-storage state, or the unrelated untracked
`knowledge/exit-ticket-game-1.1.1.zip`.

## Required next action

Commit and push the platform and lesson-repo evidence, then prepare
`GATE-L1.7B-Q2` for human review. Do not start the human interview until the
review packet and all cited evidence have been committed and pushed to the
normal remote branch with the reviewed commit/hash recorded.
