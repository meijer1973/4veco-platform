# Sprint B2-2.1.2-HARDEN: Lead Review Corrections

## Round 1 Verdict

Round 1 verdict: REQUEST CHANGES.

Lead review round 1 requested corrections before sprint closure. The findings
were accepted and applied.

## Corrections Applied

1. Duplicate assembled-chapter worked example resolved.

`build_chapter.py` now extracts only the `## Opgaven` block from `opgaven.md`
when appending exercises to a paragraph that does not already embed them. This
keeps the standalone `opgaven.md` support material intact while preventing the
combined chapter from showing the 2.1.2 smoothiebar worked example twice.

Evidence after rebuild:

- `2.1 Kosten en opbrengsten - hoofdstuk.md` contains the 2.1.2 smoothiebar
  worked example once.
- The 2.1.2 exercise section starts once at `## Opgaven`.
- Chapter validation passes.

2. Target graph-production route strengthened.

Opgave 8 now asks students to draw a `TK`-`TO` graph, put `Q` and euros on the
axes, draw both lines, mark the break-even point, and show profit at `Q = 400`
as the vertical distance between `TO` and `TK`.

The answer model now gives graph points for both lines, the break-even point
`(300, 750)`, and the vertical profit distance at `Q = 400`.

3. Imported QC disposition updated.

`2.1.2-quality-ref.yaml` now identifies the paragraph as repository-integrated
sprint evidence for `B2-2.1.2-HARDEN`, with round 1 blockers corrected and
round 2 lead review pending.

`2.1.2-review.md` was replaced with a repository sprint disposition that names
the applied corrections and carry-forward flags.

4. Command evidence consolidated.

The sprint command runner initially logged a few validator commands beside the
lesson folders because they were run from those folders to avoid Windows path
quoting failures. Those real runner entries were consolidated into
`reports/sprints/B2-2.1.2-HARDEN-command-log.jsonl` and
`reports/sprints/B2-2.1.2-HARDEN-command-log.md`. The stray lesson-side
`reports/` folders were removed after consolidation.

## Validation After Corrections

After corrections and rebuild:

- `node build-scripts/sprints/check-sprint-plan.js reports/sprints/B2-2.1.2-HARDEN-plan.md` passed.
- `node build-scripts/sprints/check-sprint-bundle.js B2-2.1.2-HARDEN` passed.
- `node build-scripts/sprints/check-scope-language.js --active` passed.
- `node build-scripts/references/check-roadmap-version-index.js` passed.
- Corrected 2.1.2 paragraph validation passed.
- Corrected Chapter 2.1 validation passed with one warning: chapter PDF size 232 KB.
- Corrected Book 2 health check passed 5/5.
- `git diff --check` passed with LF-to-CRLF warnings only.
- `git -C ../4veco-lessen diff --check` passed.

## Round 2 Readiness

The sprint is ready for round 2 lead review. The reviewer should recheck the
duplicate assembly finding, the Opgave 8 graph-production route, the QC
disposition, command-log evidence, and the closure packet once result and diff
artifacts are present.
