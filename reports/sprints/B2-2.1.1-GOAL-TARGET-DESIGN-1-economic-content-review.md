# B2-2.1.1 Economic-Content and Mathematical-Precision Review

Review mode: separate read-only Codex economics specialist; not human owner
approval.

## Round 1

- Verdict: **REVISE**
- Goal hash: `bcfb56a070ee0535dac8f2291d019b369dbe6cd9d0abe688a2bd3f73bb78a913` (verified)
- Target hash: `1f6c966d439796050821d0d6d8b1f45c68e279be6e581333ee431999a68e6e67` (verified)

Blocking findings:

1. The oven-electricity sentence determined only a slope. With oven costs `x`
   at 500, the prompt allowed `TVK(500)=350+x`; the answer key's
   `TVK=0,80Q` therefore relied on an unstated intercept.
2. `D02` and `D26` conflicted with the break-even/`MO=MK` and formal
   cost-shape non-goals, while exact-fit `A50`, `A86`, and `D38` were absent.
3. `D1.21`, `A2.11`, and `A2.15` were not sampled; `A2.10`, `D1.16`, and the
   total/average portion of `D1.17` were defensible.
4. A prospective `reviewed_final` record needed current replacement-review
   provenance inside the hashed payload or an explicitly validated adjacent
   mechanism.
5. The 12–14-minute aim conflicted with a conservative 20–26-minute estimate
   and required a fresh timed walkthrough rather than assertion.

Under the intended model, the numerical audit itself was correct:

| Q | TCK | TVK | TK | GCK | GVK | GTK |
|---:|---:|---:|---:|---:|---:|---:|
| 500 | €500/month | €400/month | €900/month | €1,00/loaf | €0,80/loaf | €1,80/loaf |
| 1.000 | €500/month | €800/month | €1.300/month | €0,50/loaf | €0,80/loaf | €1,30/loaf |

The review also required exact Dutch goals in the Markdown package, consistent
Dutch number notation, explicit handling of the two missing totals at 1,000,
and an answer-form trace to `A96`/`A98` without silently adding those IDs to the
content-skill array.

## Round 2

- Verdict: **REVISE**
- Goal hash: `33520c77cdf17a518e93a1be7dc1da16821ce2e22bfb53fe75eb0bbba169c1f1` (verified)
- Target hash: `e6c3703397eaf2f91cd5cb29028b08366efe4dcc7df33039512b29b5351de3cb` (verified)

All original economics/precision blockers closed. One new blocking load finding
remained: d required four measures at 1,000 and e then required the other two,
so the claimed reduced second set was actually a second complete set. The
reviewer required supplying `TCK=€500/month` and `TVK=€800/month` for e rather
than making students write two further calculations. It also required named
formula → substitution forms in the answer audit.

Non-blocking carry flag: protected live unit `D38` depends on semantically broad
`D02`; this package must not import `D02`'s break-even/`MO=MK` content. A later
protected-registry dependency-fit review owns that issue.

## Round 3

- Verdict: **PASS**
- Goal hash: `afda7a196ae5ea9e1d23207158c33c1b32c5f5ebc928752c3fe4b54d5172a560` (verified)
- Target hash: `709535d15ab3c89b7cfe3bac27ae9a152044cbd7611057b3bdf0defec1cc3f34` (verified)
- Exact equality of goal statements and target `lesson_goals`: verified

The prompt now contains one complete `Q=500` set and a genuinely reduced
`Q=1.000` set. Question e supplies `TCK=€500/month` and `TVK=€800/month` only
as comparison inputs; it does not require duplicate calculation. The six
economic judgments, spreading constant costs, and the bounded reason for
constant `GVK` remain fully elicited.

All numbers, formulas, units, classifications, the
`A08`/`A14`/`A50`/`A86`/`D38` mapping, two missing-unit flags, and
`A2.10`/`D1.16`/bounded-`D1.17` exam-code mapping pass. The answer audit now
models both the named formula → substitution form and the literal compact
`judgment — change — cause` form.

No independent economics, mathematical-precision, or structural response-load
blocker remains. Timing is separately supported by the final blind 13.25-minute
central calibration.

Non-blocking carry flag: protected unit `D38` depends on semantically broad
`D02`. This target does not import `D02` or its break-even/`MO=MK` content; a
later protected-registry dependency-fit review owns that issue.
