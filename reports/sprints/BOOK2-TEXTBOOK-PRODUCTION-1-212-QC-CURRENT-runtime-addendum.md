# §212 QC runtime correction, before controlled regeneration

Actor: `paragraph_214_builder`; role: `independent212specialistQC`.
Task: `BOOK2-TEXTBOOK-PRODUCTION-1-212-QC`. Root remains sole integrator.

The original dispatch required process-only MSYS-FIRST Python314, and the
committed operational plan faithfully selected it. Fresh full r19 exited zero,
but original-artifact parity failed. Its 20 changed generated files, unchanged
source/SVG/Markdown, exact invocation/environment, PENDING manifests and all 27
page images are retained without alteration. They are not passing QC evidence.
The failure inventory raw hash is
`338274b4310d1261c010573704bdd8ee09c15c2e94b1ed7af9a1025278c87433`.

Root subsequently clarified that MSYS-FIRST is paragraph-223-specific and that
§212 original root r16/full, r17/thin and r18/print used inherited PATH. Root
explicitly authorized one ordinary full native regeneration from precisely the
captured failed-r19 state, using that original inherited environment, after a
fresh global immutable proof revision reservation. No other state is allowed.

Read-only single-SVG experiments wrote only this task's evidence. Both used
Python 3.14.3, CairoSVG 2.9.0, cairocffi 1.7.1 and Cairo 1.18.4, but the DLLs differ:
inherited PATH selected bundled Poppler Cairo/font libraries; MSYS-FIRST selected
MSYS Cairo/font libraries. Inherited output equals the published first figure
in bytes and decoded pixels; MSYS output equals the failed-r19 first figure.
Version strings alone do not identify the reproducible rendering backend.

Before regeneration the helper must check every original platform input,
historical §212 report and tracked lesson file. Only the exact 20 output hashes
in the immutable failure inventory may differ for this single prebuild check.
After regeneration, original 34-artifact byte equality, 27 raw/decoded page
images, 11 native PNGs and ZIP membership checks remain strict. No copying,
restoration, source edit, guard change or drift waiver is permitted.

If the full regeneration reproduces the originals, continue full/thin/direct
print/checker and personal inspection in that recorded inherited environment.
If it fails, stop further regeneration and report the precise remaining delta.
The original operational plan and failed r19 evidence are never retroedited.
This authorizes a runtime-correct native recovery, not any QC or root acceptance.
