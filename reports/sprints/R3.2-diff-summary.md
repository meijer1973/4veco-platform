# Sprint R3.2: Diff Summary

## Summary

R3.2 completed the authorized CLI-only mutation set after fixing the dependency layer recomputation workflow.

## Protected surfaces

Protected reference data changed only through CLI:

- `references/machine/micro-teaching-units.md`
- `references/machine/micro-teaching-units.json`

No files changed under:

- `references/external/`

## Implementation diffs

- `build-unit-index.js` now supports a `skipStoredLayerValidation` option and parses JSON-quoted strings correctly.
- `unit-lib.js` now validates non-layer invariants, recomputes effective layers, validates again, and writes the final recomputed markdown/JSON.
- `unit-add-dep.js` and `unit-update.js` support `--dry-run`.
- `build-scripts/references/README.md` documents dry-run usage.

## Registry diffs

- Added 15 approved prerequisite edges.
- Marked `A01`, `A02`, `A03`, `A04`, `A05`, and `A38` as `underbouw_assumed`.
- Added `assumed_prior_knowledge` and `zero_needs_review` metadata to those six A-domain units.
- Updated affected derived layers.

## Report diffs

- Regenerated `reports/dag-integrity.md`.
- Regenerated `reports/needs-coverage.md`.
- Regenerated `reports/unresolved-refs.md`.

`reports/needs-coverage.md` now reports 48 live units with no `needs`.
