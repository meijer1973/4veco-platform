# Sprint R3.2: Diff Summary

## Summary

R3.2 recorded the human mutation authorization and attempted the required dry-run pass. The sprint stopped before protected reference mutation because the dependency CLI failed validation on the second approved edge.

## Protected surfaces

No protected reference data changed:

- `references/machine/`: unchanged
- `references/external/`: unchanged

## Implementation diffs

- Added dry-run support plumbing to `unit-lib.js`.
- Added `--dry-run` behavior to `unit-add-dep.js`.
- Added `--dry-run` behavior to `unit-update.js`.
- Updated reference CLI README with dry-run usage.

## Report and planning diffs

- Added R2.4 mutation-review decision record in Markdown and JSON.
- Added R3.2 sprint plan, baseline, mutation log, result, diff summary, and result JSON.
- Updated the roadmap to show R3.2 stopped during dry-run and to route the next action to a CLI layer-recomputation fix.
