# §224 QC additive direct-destination safety order

2026-09-06, actual actor paragraph_214_builder, same owned task and pair.
Root's independent controller reading identified a real orchestration omission:
the generic print_pipeline.py writes PDF output before its proof-directory
existence check. Our direct r12 orchestration ran the explicit specialist and
durable gates and source/input checks, but did not itself reject a preoccupied
direct proof destination before invoking that primitive.

Preserve all successful r10–r13 records and controller/source versions exactly.
Do not relabel r12 as having a new pre-effect check. Add a direct destination
preflight in this owned controller, before preflight-record, gate or worker
effects. Reject either a file or directory already occupying the exact
E/224-direct-<revision> path, even an empty directory. Guard normal revision
syntax and exact owned containment. No deletion, restoration or fallback.

Commit the corrected whole controller before execution and use its actual new
commit as external guard anchor. Add separately named temporary technical
namespace fixtures through the real run() entry: existing empty directory,
populated directory, and file must fail before save/command/native methods.
Also verify the actual unused positive namespace and all unchanged current/
committed source inputs. This is preflight-only positive evidence, not a new
native reproduction claim. Prior genuine r12 parity is distinct historical
positive execution; no need to rewrite unchanged pupil PDFs merely to add a
read-only destination check. If targeted behavior needs a real new run or
reveals drift, stop and report root before any wider change.

No shared primitive, pupil source, plan, target, canonical review, root manifest
or handoff changes. Final report and QC bindings name the new safety evidence
and distinguish authorized orchestration from the ungated generic primitive.
