# Pre-native controller count correction

2026-09-06, paragraph_224_builder. The first actual focused run passes23 tests:
all17 original methods plus six new succession methods, not seven methods.
The six methods include14 real missing/forged cases and the additional whole-
source/guard mutations; method count and subcase count are distinct.

Before any native build, correct only the controller's prospective result count
7→6. Its immutable baseline already records controllerff53e927 and all12352
platform/1902 lesson inherited files. Keep that baseline byte-exact. A corrected
controller commit is independently supplied on subsequent command lines and
whole-byte compared to its Git blob. Compare the baseline's generator binding
to the current guarded generator instead of falsely requiring the older
controller commit field to be the new controller commit. Both versions are
retained in Git; no source/test/generator/pupil/pin change accompanies this note.
Every original-file preservation check remains exact. The actual first test
log prebuild-focused-tests.json is retained unchanged.

The first full inherited snapshot was captured after the five exact generator
substitutions, before native sideeffects. It independently derives the original
generator from fixedP50db4c5d and compares every other inherited file to exact
P50db4c5d/L42996c60 Git blobs plus captured raw bytes. This is explicit execution
order, not a claim that the current changed generator was an original baseline.
