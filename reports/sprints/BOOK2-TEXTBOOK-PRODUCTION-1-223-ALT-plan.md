# §223 B223-ALT-01 exact metadata correction plan

Date: 2026-09-06. Task BOOK2-TEXTBOOK-PRODUCTION-1-223-ALT.
Actual actor paragraph_231_specialist_qc; role 223metadataauthor. This is
source authorship, not an independent paragraph review or specialist QC. Root
alone integrates and accepts. This plan is committed before source changes.

## Input and scope

Owned pair: C:/wt/book2-223-alt-correction-20260906/{4veco-platform,4veco-lessen}.
Branch: agent/book2-223-alt-correction-20260906 in both repositories. Exact
published QC input: platform e4fc984c9cb28c6f03d0f3040136af73315ca916 and lessons
6663532621e1347c12f691862ee85200665ad14f. Both claims and clean checks passed;
governance freshness passed against main 96416b6b5bd57094576e9aba0a42d682584ec479.

Correct only B223-ALT-01: theory.md figure 2 and exercises.md figure 4 currently
reuse imperative full captions as image alternatives. Keep those full captions,
all printed teaching/answers/targets, annual income, Ei boundaries, resetting
the baseline, 16 target points, bonus and 54/69/81-minute route unchanged. Keep
all four accepted §221 pins, the paragraph and chapter plans, generator and
shared print pipeline byte-exact. No target, governance, review, QC, handoff,
other-paragraph or Part B changes. The existing QC REVISE remains immutable.

## Read authority and native syntax

Both AGENTS, three strategic documents, textbook end state and lane workflow,
BUILD-PARAGRAPH common/Part A sections, BUILD-CHAPTER and build README were read.
The five paragraph/didactics/exercise/graph/PDF skill main files and external PDF
skill were read, together with figure/rendered-page/accessibility standards,
school-fit and pedagogical boundaries. The canonical Book 2 outline and metadata,
full §223 and chapter 2.2 plans, QC report/publication/personal observations,
semantic-alt findings and probe implementations, full four authored sources,
generator, shared pipeline, checker, original tests, successor tests and native
thin wrapper were read. Embedded PNG base64 in QC finding JSON is binary evidence,
not a claimed prose read. Existing seven original tests remain exact.

§211 and §212 already use the same shared pipeline with native Pandoc
`{alt="..."}` attributes. Read-only prepare_html probes verified that syntax
preserves all visible caption words and changes the short alternative. Pandoc
also removes aria-hidden from the now-distinct figcaption and rewraps its HTML
line whitespace. Those explicit consequences, not arbitrary HTML changes, are
permitted. Two initial probe assertions were too strict (caption DOM equality,
then internal line whitespace); neither wrote a file. The repaired probe passed
after checking the exact visible words and the precisely identified metadata.

Planned exact alternatives (109 and 118 characters):

- Figure 2: `Ei-schaal: inferieur bij Ei<0, normaal bij 0<Ei<1 en luxe bij Ei>1; open grenspunten 0 en 1 zonder categorie.`
- Figure 4: `Drie scenario's: beginsituatie, alleen hoger inkomen en terug naar dezelfde basis voor alleen een hogere andere prijs.`

## Quality floor and fixed-byte guard

Preserve the advantage in auditable native reproducibility and economic precision
and parity in paper usability, caption completeness and functional accessibility.
No shared-pipeline change or generated-HTML repair is planned. Append one explicit
alt attribute to each of two exact source image lines. Derive each complete expected
source file from immutable platform 3510fc4dd30c9c01f44111ecc022ae239e855758 using
only the two fixed before/after byte literals, with unique-match assertions.
Retain the existing whole-generator four-pin derivation and every unchanged-file
guard. Update successor tests only to call that explicit source derivation.
Add negative regressions for original imperative alts, unrelated source/generator/
original-test edits, missing/forged accepted inputs with real hashing, full-caption
drift, long and imperative alternatives. Record the complete allowed test-controller
delta for independent review; a current-hash allowlist or broad stripping is forbidden.

## Execution and evidence gates

1. Commit this operational plan before source/test/helper edits.
2. Implement the two authored attributes and explicit complete-byte source guard.
   Run original and successor tests plus added negative metadata regressions.
3. Scan both repositories' actual registered worktrees, standard and nested proof
   history and reservation records. Reserve and announce each fresh §223 revision
   above 18 before full, thin and direct-print native builds. Preserve all earlier
   reservations, PENDING manifests, r5 and r17 failures and command diagnostics.
4. Immediately before first PDF authoring, run the external PDF skill's artifact
   marker. Use explicit C:/Python314/python.exe and MSYS-first child PATH only;
   no global runtime mutation. Native builds regenerate, never manually restore.
5. Verify three PDFs byte-exact to the input, 15/10/7 pages, all 32 fresh raw PNGs
   and decoded pixels exact to prior QC, four SVG/PNG pairs byte/pixel exact, and
   20 native files accounted for. MD permits only two literal attribute insertions;
   HTML permits only three exact short-alt/caption metadata transformations. ZIP
   permits only those members' bytes, with safe unique 11/5/3 names and passing CRC.
6. Run the actual checker with rebuild, thin native wrapper, direct native print,
   authority/currentness/durable and normal lesson profiles/bundle gates. Capture
   actual subprocess command, return code, stdout and stderr in unique evidence.
   Personally inspect every final PDF page and the relevant native figures; this
   is author verification, not a fabricated independent review.
7. Publish whole-file delta, source/native hashes, proof inventory, strict owned
   scope and genuine incremental/complete native scopes with honest failures.
   Never widen a classifier or add a fake source anchor. Original review and QC
   stay unchanged; root_validation/root_acceptance/handoff_renewal remain PENDING,
   readiness false and timing UNOBSERVED. No current full CI or Inspectie claim.
8. Commit source/native/evidence payload, then four explicitly paired generated
   indexes in a terminal commit. Normal push both branches, clean claimed pair and
   exact remote equality; return exact commits for different independent reviewers.

No PR creation or merge is authorized here. Any printed-byte drift, broader source
need or authority defect is a blocker to report, not permission to widen this plan.
