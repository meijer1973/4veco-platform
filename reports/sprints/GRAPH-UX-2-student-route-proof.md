# Sprint GRAPH-UX-2: Student Route Proof

Generated: 2026-05-31

## Scope

This proof records the student-visible GRAPH-UX-2 route after implementation
and controlled Book 1 regeneration.

The proof covers the `1.1.3 Grafieken en tabellen` graph/table practice route
only. It does not authorize target-equivalent checkpoint publication,
diagnostics, adaptive routing, mastery, sequencing, student-facing AI,
summative use, PV projection, PV machine promotion, Scale Gate 1, or
student/product use.

## Generated Route

Generated Book 1 route:

- `../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod/1.1 Hoofdstuk Economisch denken en rekenen/1.1.3 Grafieken en tabellen/1.1.3 Grafieken en tabellen – grafiekenspel.html`

The graph shell is generated by:

- `build-scripts/platform/build-graphical-shells.js`

The `1.1.3` graph data is generated by:

- `build-scripts/content/book-1/b1-113-graphical-data.js`

Generated output was refreshed by:

```bash
node build-scripts/content/book-1/b1-113-graphical-data.js
node scripts/deploy.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"
```

No generated lesson output was hand-patched.

## Student-Visible Route State

The rendered route shows:

- title: `Grafieken en tabelwaarden lezen`;
- paragraph goal: table and graph data can be selected, read, and used;
- visible route panel: `Oefenroute Grafieken`;
- current focus: `Tabelwaarden selecteren voor berekening`;
- local-progress language only: `Alleen lokale oefenvoortgang. Geen diagnose,
  beoordeling of automatische route.`;
- shared task-shell cue: `Lees de bron, geef je antwoord en kijk rustig na wat
  je volgende stap is.`

The first visible task is a table-value selection task rendered through the
shared task shell, with `data-graph-task-shell="GRAPH-UX-2"` and
`data-task-family="table_value_selection"`.

## Task Family Coverage

The generated `1.1.3` graph route now includes seven tasks:

| Coverage need | Evidence in route |
|---|---|
| table-value selection | table-value choice task with source table |
| graph reading | bar and line graph reading tasks |
| economic axis convention | graph-construction substitute task for axis meaning |
| interpolation | less-labelled line-graph interpolation task |
| point placement | x/y point placement task |
| graph-construction substitute | axis/graph construction substitute family |
| calculation/work capture | percentage-change calculation task through the task shell |
| less-labelled variant | line graph task with `show_value_labels: false` |

## Live Browser Check

The in-app browser was opened against a local HTTP server serving the generated
Book 1 route.

Observed live state:

```json
{
  "title": "Grafieken en tabellen - Grafiekenspel",
  "hasRoute": true,
  "hasShell": true,
  "family": "table_value_selection",
  "buttonText": "Controleer",
  "consoleErrors": []
}
```

The visible browser text included the route panel, paragraph goal, current
focus, local-only boundary copy, first graph task, and task-shell control.

After the first task was answered in the live browser, the active focus target
was the labelled feedback region:

```json
{
  "activeElementId": "g-task-feedback",
  "feedbackRole": "status",
  "feedbackLive": "polite",
  "feedbackLabel": "Feedback op je antwoord",
  "consoleErrors": []
}
```

The feedback focus uses `preventScroll: true` so keyboard/screen-reader focus
does not create a sticky-header scroll jump on mobile.

## Deterministic Checker

Command:

```bash
node build-scripts/sprints/check-graph-ux2-route-output.js "../4veco-lessen/Boek 1 - Grondslagen, vraag en aanbod"
```

Result:

```text
GRAPH-UX-2 route output OK (7 graph tasks; 5 required families)
```

The checker proves:

- graph pages load `task-shell.css`, `task-shell-engine.js`, and
  `task-shell-ui.js`;
- deployed graph UI renders `TaskShellUI.renderTask`;
- task-shell feedback runs through `GraphicalEngine.evaluateTaskShellResponse`;
- all required graph/table families are present;
- graph feedback is rendered in a labelled live region and focus moves there
  after checking;
- no `1.1.3` exit-ticket page is published;
- checkpoint-style graph fixture remains non-target-equivalent.

## Product Boundary

No visible route text claims that the student has completed the paragraph
target exercise or proven long-term mastery. The route remains local practice
and checkpoint-language preparation only.

The next product-quality judge is still `GATE-ENGINE-1`; the next
target-equivalent checkpoint authority remains `L1.7B-Q2` plus
`GATE-L1.7B-Q2`.
