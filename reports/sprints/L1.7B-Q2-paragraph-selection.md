# Sprint L1.7B-Q2: Paragraph Selection

Generated: 2026-06-01

## Decision

Select `1.1.2 Percentages en indexcijfers` as the first
target-equivalent exit-ticket implementation candidate.

## Rationale

`1.1.2` is the lesson roadmap's recommended first paragraph for Q2 because it
requires calculation input, final-answer notation, percentage/index
distinctions, and unit/notation handling while avoiding graph drawing.

The source audit supports that choice:

- there is no existing `1.1.2` advisory short-check source file to preserve as
  a separate surface;
- current math route work already gives useful A38/A39 practice evidence;
- the target exercise has a clear four-part operation chain;
- the known blocker, D31, can be planned explicitly instead of hidden.

## Rejected For First Q2 Pass

| Paragraph | Reason not selected first |
|---|---|
| `1.1.1` | Existing `Korte check` must remain advisory, the generator currently has one exit-ticket surface per paragraph, and the final compare/explain still needs A98 versus held-evaluation resolution. |
| `1.1.3` | Target-equivalent proof is blocked by the graph-axis convention repair and A81 source-use plus underlying answer-form coverage. |

## Conditions On This Selection

The `1.1.2` path is sound only if implementation resolves these issues:

1. D31 must be checked as student action, not only described in feedback.
2. The student must show calculation work and final answers for subquestions
   a-c.
3. The D31 answer must distinguish four index points from about 3.7 percent.
4. Generic self-check may not be counted as proof unless a reviewed rubric or
   deterministic check is added.
5. Completion language stays blocked until `GATE-L1.7B-Q2`.

If any condition cannot be met, `L1.7B-Q2` must pause rather than publish a
weak exit ticket.
