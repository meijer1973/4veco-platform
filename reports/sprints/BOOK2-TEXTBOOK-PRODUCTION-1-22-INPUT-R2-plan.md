# Chapter2.2 complete input candidate R2 — explicit heading compatibility

2026-09-06, codex-root; operational continuation of22-HEADING-COMPAT-plan.md
committed ad29db691c844b4f6bb82eaaf7c07b724b7a24d5. Same dedicated root pair,
same umbrella task/branch, no additional owner decision or production release.

Keep the complete original22-INPUT-CANDIDATE-plan/check/manifest/results
immutable. All its quality, teaching, asset, paper, timing and authority gates
apply. R2 creates new own files; it does not update the old candidate's digest.
Use the actual next committed source/evidence pair as immutable R2 Git base.

Derive all original138 selected rows again. Prove136 unchanged complete raw/LF/
blob identities and exactly two intentional changed source rows: chapter_pipeline
and test_chapter_pipeline. Bind the original candidate/checker plus complete
heading correction plan/probe/controller/original failure/passing tests/actual
passing prepare/result (nine additional files). Total147 complete input rows.
Add each actual source's exact first H1 to all eight document inventory rows.
Require explicit exact headings in the output contract; the future spec uses
those committed values, not a discovery-based authorization.

The new deterministic checker retains the original whole immutable Git/raw/
semantic consumer and all original complete current acceptance predicates;
only its own namespace, source refs, heading inventory and explicit predecessor
continuity change. Original registry/owner releases and twelve goals stay exact.
Add full offered-heading-map corruption cases. Repeat all147 missing/forged/
synchronized complete-row cases, existing manifest cases, semantic probes and
15 actual authority/profile gates. Record in-memory/OS boundaries honestly.

Verify compact raw Git custody against the pre-heading published P9efd181d and
unchanged L3bc9e818, excluding only the two declared shared source files and four
indexes. All old R1 evidence, canonical lessons,29PDFs and manifest are unchanged.
Publish combined compatibility/R2 scope and result plus exact terminal indexes.
Distinct independent review judges the entire current147-input candidate AND
the two-file compatibility source, not merely the corrected H1 or a subset.
No assembly or native acceptance until that PASS and a separate root release.
