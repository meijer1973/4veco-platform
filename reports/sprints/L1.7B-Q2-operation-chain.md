# Sprint L1.7B-Q2: Operation Chain

Generated: 2026-06-01

## Target Exercise

Source:
`references/authored/course-target-exercises.json` record `1.1.2`.

The target exercise asks students to calculate and explain percentage and
index-number changes:

1. price rises from EUR 800 to EUR 920;
2. basket price rises from EUR 150 to EUR 162 with base year index 100;
3. index rises from 108 to 112;
4. a student incorrectly says 108 to 112 means 4 percent inflation.

## Required Operation Chain

| Step | Target operation | Expected answer model | Required task family | Answer-form lane |
|---|---|---|---|---|
| a | Calculate percentage change from EUR 800 to EUR 920 | `(920 - 800) / 800 * 100 = 15%` | calculation work plus deterministic final answer and percent notation | `A96` plus A38 |
| b | Calculate 2025 price index from EUR 162 and EUR 150 | `162 / 150 * 100 = 108` | calculation work plus deterministic final answer and index notation | `A96` plus A39 |
| c | Calculate percent change from index 108 to 112 | `(112 - 108) / 108 * 100 = 3.7%` rounded sensibly | calculation work plus deterministic final answer and percent notation | `A96` plus A38/A39 |
| d | Explain why 108 to 112 is not 4 percent | The change is 4 index points; percent change uses old index 108 as denominator, so the correct change is about 3.7 percent | short constructed response or structured reasoning with D31-specific rubric | D31 plus `A97`/`A98`-style explanation criteria |

## Same-Level Requirement

The exit ticket may not reduce these operations to recognition choices. The
student must perform the calculations, enter final answers, and produce or
check the D31 explanation at the same level as the target exercise.

## Blockers To Resolve Before Proof Metadata

| Blocker | Why it matters | Required resolution |
|---|---|---|
| `calculation_work_capture` is currently self-check | Work capture alone does not prove the final numeric answer is correct | Add deterministic final-answer matching or pair with deterministic final-answer task without exceeding the task sequence quality floor |
| D31 explanation is constructed response | Generic self-check is not answer-form proof | Add D31-specific rubric/criteria and have `GATE-L1.7B-Q2` decide whether it justifies local proof |
| Current metadata name uses target-readiness wording | Product spec now says target-equivalent proof | Either update the internal status vocabulary or explicitly document compatibility while keeping student-facing wording target-equivalent |
| Completion copy is held | The human gate has not approved completion language | Keep `completionLanguageEligible: false` and use checkpoint-only completion copy |

## Minimum Future Task Sequence

The implementation should produce at least four tasks:

1. percentage change calculation from EUR 800 to EUR 920;
2. price-index calculation from EUR 150 to EUR 162;
3. index-to-index percentage calculation from 108 to 112;
4. D31 explanation distinguishing index points from percent.

A fifth notation task may be added only if it improves proof quality without
fragmenting the target exercise into artificial micro-steps.
