# §231 root publication checkpoint

Date2026-09-06. Root codex-root. Branch in both paired repositories:
`codex/book2-part-a-production-20260905`; owned worktrees under
`C:/wt/book2-part-a-production-20260905`.

Actual root candidate payload: platform
`06b99d84cfb030388af1be1ef1d2a43dfe36d813`; current lessons
`219a977e495abe43c17949e7d8996aab4176faa0`.
Root-candidate-result.md contains complete independent/source/import/rebuild
lineage and current limitations; root-final-bindings.json passes919 bindings.

At the actual committed payload, root-publication-scope.json records all four
native results, without an exception: root231 platform PASS9source+813evidence;
root lessons PASS44Part A files; genuine complete platform PASS78source,
6indexes,2896evidence; genuine complete lessons PASS202Part A+4evidence.
Every range has0 UNKNOWN. Strict incremental root scope enumerates all822 paths.
The separately preserved original author candidate scope FAIL is not relabelled.

## Preserved publication diagnostic

The first own scope wrapper incorrectly demanded whole-import default
`git diff --check` success after importing immutable reviewer Windows evidence.
It failed after the native scope checks, before writing its exclusive output.
The next attempt checked only the first displayed diagnostic file and still
failed: the complete default output also named the reviewer's CRLF
`231-review-probes-result.json`. A full path inventory then resolved the issue.

The final helper preserves complete actual default and per-command cr-at-eol
stdout/exit codes. Default whole-diff whitespace still FAILS only in the exact
imported reviewer command-log Markdown and probes-result JSON. cr-at-eol still
FAILS for the spaces after `const` in a preserved failed-command excerpt on
line696 of that log. Both files match their exact original Git blobs. All
other imported/source/root paths and the complete lesson delta pass the
ordinary whitespace check. This is an explicitly retained historical-evidence
diagnostic, not a scope failure or globally relaxed whitespace setting. No
author/reviewer evidence was reformatted or overwritten. The active workflow's
plain `git diff --check` tests uncommitted build drift, not historical committed
ranges; no current hosted CI result is inferred from that source reading.

## Publication and continuation contract

Commit this actual scope evidence and root-only diagnostic report; refresh the
URL index and four paired repository indexes using explicit lesson root,
source HEAD219a977 and source branch. Their terminal commit contains only the
four deterministic index files. Fetch/push normally, validate exact remote
heads and clean claimed pairs; do not force, bypass policy, or merge a PR.

Next assign a distinct specialist against that exact published pair. It must
personally inspect the full current33-page/15-figure packet, complete teacher,
student and visual/rendered-page QC, and create only the Part A quality record
and evidence with root validation/acceptance/handoff pending. Review hash remains
8f86129b14ef508e16f41d918299da7af2422655ff14fc9ba91b68a9b66e8943;
no current quality-ref or handoff exists. Root r14–r16 proof is immutable
PENDING; next globally unused revision starts after16. Current source/native
bytes may not be changed by the specialist. The pupil author handles genuine
corrections if found; root alone integrates and decides acceptance afterward.

The independently authored214 plan phase runs from the older valid published
572d1ea2/d4e1910 pair, which contains all reviewed211–213 teaching; unrelated
231 changes are not its dependency. Its root work order is included here for
off-site retrieval. Current212/223 dependency-delta reviews are distinct from
later specialist QC. Continue through the remaining paragraphs and aggregates.
No whole-book completion, current full-suite/CI, classroom timing/attainment or
future PR merge authority is claimed at this checkpoint.
