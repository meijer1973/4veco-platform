# Successful creation and pre-check consumer boundary

The complete candidate was exclusively created by captured r2 at committed
controllerfc53aa5aafefd93addf2bd3483e3d729a35f4376.138 inputs:54 platform,
84 lesson; rawd7cf3d9902edb65d22c4c21f31771db6b1e08db51ac8feac8dff57f400014469.
The first failed baseline was reused only after exact fresh equality, never
overwritten. r2 process exit0 and complete source/raw streams are preserved.

Before the first adversarial check, root binds the actual candidate digest in
the controller. In the same pre-check review root removes a future-consumer
mistake: existing old aggregate assets are historical destination-state evidence,
not prerequisites requiring legacy bytes to remain current after a successful
new chapter build. Their four exact old blobs/hashes remain derived from the
immutable baseline Git; every accepted source asset is still a full current
raw/Git input. Current whole baseline/preservation checks continue to protect
all old outputs during this no-native candidate phase. The candidate's138 input
rows and complete JSON bytes are unchanged by that controller correction.

This makes later honest full/thin/direct reproduction possible after the four
declared legacy assets are replaced, without accepting stale source bytes or
weakening source/authority/acceptance bindings. No native run demonstrates that
future behavior here; genuine production-entrypoint tests remain required.
No new failed attempt occurred for this before-use correction.
