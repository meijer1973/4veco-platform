# Sprint L1.7B-Q2: Source Data Contract

Generated: 2026-06-01

## Future Source File

Future implementation may create:

```text
source-data/book-1/exit-ticket/1.1.2.json
```

This planning step must leave that file absent.

## Required Surface Metadata

The future source data must identify itself as a target-equivalent candidate,
while keeping gate approval and completion-language eligibility false:

```json
{
  "surface": "target_equivalent_exit_ticket",
  "targetEquivalent": {
    "candidate": true,
    "gateApproved": false,
    "completionLanguageEligible": false
  }
}
```

If the existing engine still requires
`metadataAlignment.status: "target_exercise_readiness_aligned"` for
compatibility, implementation must document that as legacy internal vocabulary.
Preferred implementation is to update validation so a target-equivalent status
can be used without weakening compatibility.

## Required Skill Metadata

The source data must cover the full target-exercise skill set:

```json
{
  "targetSkillIds": ["A38", "A39", "D31"],
  "skillScopeIds": ["A38", "A39", "D31"],
  "metadataAlignment": {
    "paragraphSkillIds": ["A38", "A39", "D31"],
    "targetExerciseSkillIds": ["A38", "A39", "D31"],
    "targetReadinessEvidence": true
  }
}
```

Student-facing labels must not expose these internal IDs.

## Required Task Contract

Every target operation from `L1.7B-Q2-operation-chain.md` must map to a
source task.

| Operation | Required source-task behavior |
|---|---|
| EUR 800 to EUR 920 percentage change | Requires visible calculation/work and final answer `15%` or accepted equivalent. |
| EUR 150 to EUR 162 price index | Requires visible calculation/work and final answer `108`. |
| 108 to 112 percentage change | Requires visible calculation/work and final answer about `3.7%`. |
| D31 explanation | Requires criteria: says `4` is index points, not percent; uses old index `108`; gives about `3.7%`; does not call the claim correct. |

## Completion Copy Before Gate

Before `GATE-L1.7B-Q2`, allowed completion copy is limited to checkpoint-only
language such as:

```text
Je hebt deze check afgerond.
```

Held until the human gate:

```text
Je hebt laten zien dat je de eindopgave van deze paragraaf aankunt.
Je kunt nu door naar de eindopgave.
Je hebt deze paragraaf-check succesvol afgerond.
```

## Validator Requirements

Future implementation validators must fail if:

- the source omits any of the four target operations;
- D31 is only mentioned in feedback and not checked as a task;
- calculation work is captured without a final-answer check;
- a choice-only task replaces a calculation or explanation required by the
  target exercise;
- student-facing text contains internal MTU IDs;
- completion copy is shown before `GATE-L1.7B-Q2`;
- diagnostics, mastery, sequencing, summative, AI, PV, Scale Gate, or product
  claims appear.
