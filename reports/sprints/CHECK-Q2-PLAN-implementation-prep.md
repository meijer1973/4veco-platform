# Sprint CHECK-Q2-PLAN: Implementation Prep

Generated: 2026-05-31

## Purpose

Prepare the implementation requirements for later `L1.7B-Q2`.

This file does not authorize implementation.

## Required Future Work Package

A later `L1.7B-Q2` plan must include:

1. selected paragraph and target exercise;
2. complete operation-chain table;
3. answer-form and task-shell mapping;
4. source-data contract for target-equivalent exit-ticket tasks;
5. state separation between advisory short check and proof task;
6. validator upgrades;
7. generated-output build route;
8. screenshot and live-output proof requirements;
9. teacher-learning, student-experience, accessibility, testing, and lead
   review;
10. `GATE-L1.7B-Q2` review packet.

## Candidate Paragraph Preflight

| Candidate | Minimum repair before proof implementation |
|---|---|
| `1.1.1` | Add full A43 calculation chain and decide whether final compare/explain is `A98` or held evaluation. |
| `1.1.2` | Add explicit checked D31 task for 4 index points versus about 3.7 percent; coordinate math and reasoning feedback. |
| `1.1.3` | Repair target graph convention to price vertical and quantity horizontal; add target interpolation, 50 percent interval, and A81 source-use plus explanation. |

If none of these repairs are authorized, `L1.7B-Q2` should pause or produce a
more specific implementation plan rather than generate weak proof output.

## File Ownership For Later Implementation

The future implementation plan must identify exact files before edits. Likely
owners include:

| Area | Expected owner |
|---|---|
| source exit-ticket data | `source-data/book-1/exit-ticket/*.json` through reviewed source-data route |
| exit-ticket engine/UI | existing exit-ticket engine/UI or shared task-shell wrapper |
| shared task shell | `engines/task-shell-engine.js`, `engines/task-shell-ui.js`, and CSS if a task family needs hardening |
| route panel | `engines/skill-map-engine.js` and route display helpers if proof route state needs display |
| graph tasks | graph route data/module only after GRAPH-REFINE-2 or equivalent authorization |
| math tasks | skilltree/math route data/module only after MATH-REFINE-2 or equivalent authorization |
| reasoning tasks | reasoning data/module only after REASON-REFINE-2 or equivalent authorization |
| generated output | platform deploy/build scripts only; no hand patches |
| validators | sprint-specific checker plus scope, book, platform, link/data, screenshot evidence |

## Validator Requirements

Later implementation validators must fail if:

- advisory short check uses target-equivalent proof flags or completion copy;
- target-equivalent output omits any reviewed target operation;
- choice-only tasks replace required calculation, graph/table, or short
  response actions;
- `1.1.2` omits D31 explanation;
- `1.1.3` uses price horizontal or quantity vertical for the target graph;
- `1.1.3` omits EUR 1.75 interpolation around 350;
- `1.1.3` omits the EUR 2.50 to EUR 3.00 50 percent drop using 200 and 100;
- `A81` is treated as a standalone answer form;
- held evaluation/classification lanes are used without explicit review;
- `A80`, `A81`, or `A96`-`A99` leak into interactive skill-tree exposure while
  generator-blocked/non-interactive;
- generated output exposes internal MTU IDs;
- diagnostics, mastery, sequencing, summative, AI, PV, Scale Gate, or product
  claims appear.

## Rendered-Output Proof

Future `L1.7B-Q2` closure must inspect live rendered output at minimum:

- selected paragraph landing route with advisory/proof surfaces clearly
  separated;
- full target-equivalent exit-ticket task sequence;
- one feedback state per required task family;
- mobile or narrow viewport;
- dark mode;
- final result state with completion copy still blocked before gate approval;
- next practice advice after an insufficient response.

Screenshots must be committed and pushed before human review.

## Review Requirements

Future implementation must include:

- planning/review subagent before implementation;
- testing-agent or deterministic test evidence;
- student-experience review for orientation, cognitive load, next action, and
  copy clarity;
- teacher-learning-quality review for target-operation completeness;
- accessibility review for keyboard/focus, labels, contrast, and feedback;
- lead-review assignment, round 1, correction log, and round 2;
- human gate `GATE-L1.7B-Q2` before completion copy.

## Stop Conditions

Stop later implementation if:

- the selected paragraph operation chain is not reviewed;
- a blocker is unresolved and not explicitly scoped for repair;
- implementation would require hand-editing generated output;
- completion copy is requested before `GATE-L1.7B-Q2`;
- product authority, diagnostics, mastery, sequencing, summative use, PV,
  Scale Gate 1, or student/product use is requested;
- reviewer evidence cannot inspect rendered output.

## Recommended Next Action

After CHECK-Q2-PLAN, prepare either:

- a more specific `L1.7B-Q2` plan that selects one paragraph and includes the
  required repairs; or
- explicit repair sprints such as `GRAPH-REFINE-2`, `MATH-REFINE-2`, or
  `REASON-REFINE-2` before target-equivalent implementation.

Do not proceed directly to Scale Gate 1.
