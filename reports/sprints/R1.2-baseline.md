# Sprint R1.2: Baseline

## Plan reference

`docs/sprints/R1.2-plan.md`

## Baseline summary

R1.2 starts after R1.1 established `references/SOURCE_OF_TRUTH.md`.

Missing before implementation:

- `references/schemas/`
- `build-scripts/references/validate-core-schemas.js`

## Data integrity notes

No protected reference data changed at baseline. `references/machine/` and `references/external/` remain protected surfaces and are not edited in R1.2.

## Known risks

- These schemas are first contracts, not a full migration of existing registries.
- Schema existence must not imply that all downstream data already conforms.
