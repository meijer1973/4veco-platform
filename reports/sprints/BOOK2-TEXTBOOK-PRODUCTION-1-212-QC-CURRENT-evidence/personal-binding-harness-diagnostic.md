# Personal binding instrumentation diagnostic

The first `CURRENT-finalize.py personal` invocation stopped at its ordering
assertion before creating `personal-inspection.json`. The separately authored
page observations are in actual inspection order (including answer page 4
inspected early to resolve the formula-wrap diagnostic), whereas the grayscale
inventory is in edition/page order. This is not a missing inspection or pupil
defect. The helper now joins the original observations by unique `(kind,page)`
keys, requires all 27 unique keys, and verifies each exact image hash. Neither
original observations nor native manifests were modified. No image views or
observations are inferred from this mechanical binding step.
