# Golden Exercise Workbench Rollout Goal Sequence

Generated: 2026-06-09

## Purpose

Define concrete `/goal` runs for the project.

## Goal 0 -- Preparation

ID:

```text
GOLDEN-EXERCISE-WORKBENCH-PREP-1
```

Purpose:

```text
Create durable project-control documents.
```

No implementation.

## Goal 1 -- Policy extraction

ID:

```text
GOLDEN-EXERCISE-POLICY-1
```

Purpose:

```text
Create central layout and interaction policy.
```

Scope:

```text
references/ui/*
references/exemplars index updates if needed
checkers only if lightweight and needed for policy proof
```

No migration.

## Goal 2 -- Exemplar promotion

ID:

```text
GOLDEN-EXEMPLAR-PROMOTION-1
```

Purpose:

```text
Promote the implemented 1.1.3 route as repository-compatible exemplar and link A96 as answer-form exemplar.
```

No migration.

## Goal 3 -- Checker hardening

ID:

```text
GOLDEN-EXERCISE-CHECKERS-1
```

Purpose:

```text
Generalize no-legacy and Golden workbench checks for any route opting into golden_exercise_workbench.
```

No migration.

## Goal 4 -- Renderer generalization

ID:

```text
GOLDEN-EXERCISE-RENDERER-1
```

Purpose:

```text
Make renderer selection data-driven from layout.framework, not hard-coded to 1.1.3.
```

No broad migration.

## Goal 5 -- First transfer proof

ID:

```text
EXIT-TICKET-WORKBENCH-112-1
```

Purpose:

```text
Apply Golden Exercise Workbench + A96 answer-form rules to 1.1.2 exit ticket.
```

This is the first real transfer proof.

## Goal 6 -- Short-check variant

ID:

```text
SHORT-CHECK-WORKBENCH-POLICY-1
```

Purpose:

```text
Define and prove advisory short-check variant.
```

No target-equivalence claim.

## Recommended stopping point for first Codex `/goal`

Stop after Goal 0 only.

Reason:

```text
The current request is to prepare the work. Implementation goals should start only after project-control docs are committed.
```
