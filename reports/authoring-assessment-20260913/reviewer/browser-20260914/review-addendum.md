# Independent browser review addendum — 14 September 2026

PASS for the inspected assessment scope. Reviewer: parent agent, independent of all three assessment authors. This supplements the September 13 reviews at unchanged HTML/PDF/content bytes; it does not rewrite the original trial outcome or grant student publication/integration.

- **New paragraph:** inspected all three final HTML documents from top to bottom in the actual Codex In-app Browser at 1280 × 720 CSS pixels. The 35 overlapping viewport captures cover paragraph, exercises and answers completely. Checked rendered headings, continuous text, formulas and units, subquestion letters around tables, all three figures and captions, repaired response tables, target/bonus grouping and final interleaving. No clipping, overlap, missing images or horizontal document overflow was observed. Existing mathematical/source review and all 25 final PDF-page inspections remain applicable because their bytes did not change.
- **Exercise correction:** inspected the affected exercise, answer and neighbours in three paragraph HTML files and both dependent chapter HTML files. Captures show EUR175, TCK1075 and TK1475 at Q200 correctly, with legible question/answer flow. Existing six affected PDF pages plus ten neighbours remain covered by their prior independent review. Chapter HTML retains its existing screen styling; this bounded numeric repair does not establish whole-chapter acceptance.
- **Layout repair:** reused the previously inspected actual-browser capture `layout/final-html-summary.png`. The closure preservation record verifies the unchanged HTML hash `8330a14d25718c347216dc9e5253d6734345495c61fb0af661fdc4ad4954ac31`. Existing final PDF inspection and unchanged pages 3–5 remain valid.

`binding.json` records actual file hashes and lesson heads. Named viewport captures are the primary new browser evidence; `checked-*.png` are contact sheets of those captures. DOM measurements corroborate image loading and overflow but are not substitutes for visual inspection. This checks the available intended assessment browser, not an unrequested browser matrix, deployment or classroom performance.

The browser tool's initial full-page stitched screenshots repeated/cropped portions at tile boundaries. They were rejected as capture artifacts and replaced by settled, unstitched viewport captures. They are not included as acceptance proof. Local HTTP serving succeeded here; the September 13 local-file access refusal remains part of the historical log.

The new paragraph's prior browser limitation is resolved by this dated addendum. Bounded exercise/layout PASS remains scoped; the historical exercise paragraph lacks a full current manifest, and that unrelated baseline limitation is not relabelled as full closure. Packaging and this browser work occurred after the timed trials; no faster end-to-end completion time or token saving is claimed.
