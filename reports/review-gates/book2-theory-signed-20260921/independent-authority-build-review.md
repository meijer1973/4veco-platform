# Independent bounded authority and build review

Reviewer: `/root/review_book2`; author: `codex-root`. Date: 2026-09-21. Repository access remained read-only. Controlled source mutations and generated proof files were confined to external fixtures under `review/`.

**Scoped verdict: PASS for the reviewed authority successor, active signed-elasticity consumers, current native build implementation and failure gates.** The two concrete build findings below are repaired and independently reproduced as rejecting their bad inputs. This is an interim source-level verdict, not final acceptance of the revision manifest, final commits, compatibility matrix or exact-pair CI. Historical review/approval records and existing holds retain their original meaning.

The exact 37 reviewed platform files and six evidence identities are recorded in `independent-authority-build-bindings.json`, SHA-256 `9944f2c49371f9a11b80c22d5ed3154bda8502d73f44053590c36528ea42c3c0`. The separate native-content report remains unchanged and its PDF/source bindings still matched after the latest full rebuild at this checkpoint.

## Authority and scope

The finite successor derives the four affected authority files from platform baseline `37cc57bda5096b6b8cb455fe9cd27b20acacd3fe`, checks their before hashes, applies seven specified target-field replacements and eight specified text replacements, and checks exact successor hashes. It does not rewrite the frozen Issue #229 candidate package or old owner/integration approvals. The twelve paragraph identities/order, target contexts, data, labels, points, statuses and unrelated records remain protected. The target wording is the authorized signed-convention change, not a new economic operation.

The metadata projection changes only the semantic outline hash, exact current target pins and authority-source currentness. Full metadata identity is required where the successor is admitted by the durable lifecycle checks. Ordinary paragraph-production and whole-book holds still fail with the expected `H-221-PRIOR` and `H-BOOK2-ROOT-PLAN` holds. No `candidate_review_ready` target has been promoted to `reviewed_final`; the existing Ei owner decision and historical approval subjects are preserved. The blueprint now distinguishes historical cinema/petrol promotion from the current Nova/StreamNow candidate rather than transferring approval implicitly.

The Books 3/4 migration/currentness hooks admit only the exact new Book 1/2 record-set hash or exact registry/blueprint file hashes. The current revision checker has a pinned complete inventory, checks old Git blob identities, rejects added/removed/changed unlisted files, preserves original route evidence, and prohibits Books 3/4 and historical-receipt changes even under a newly recorded manifest. Its finite lane path set classifies ownership; it does not replace the byte/closure gate. Final manifest and path-pin identities remain for final review.

The live RX.4 checker now requires signed boundaries, zero, unrounded classification, old denominators, undefined zero-price-change handling, context caution, and bounded local/finite turnover reasoning. Existing IDs, dependencies, generators and product blocks remain checked. The historical reseeding CLI refuses to write when any authorized RX.4 unit already exists, preventing it from restoring obsolete procedure text. Its sealed historical inputs remain unchanged.

GEN.A15 uses the actual old-value percentage calculations and classifies the unrounded signed result; the current generator retains its existing numerical domain and exercise operations. The B8 explanation and graph-arena hint follow the same convention. No blocked RX.4 generator was implemented or activated. The unit and term mutations remain covered by the earlier independently reviewed CLI/projection reports.

The skill-tree viewer is a stale generated cache refreshed through its owning tool. The independent reviewer ran that actual generator in a VM with only its output write intercepted and compared the complete generated HTML with the current file: exact equality. Its expansion from 217 to 257 cached records and 46 changed/new cached IDs reflects the existing catalogue plus the four authorized unit edits, not 40 newly authored units or generator activations. The evidence is `independent-viewer-projection.json`.

## Source ownership and build

Runtime student rendering consumes the native manuscript PAGE blocks, semantic HTML components and bounded figure specifications. It does not read supplied target PDFs to manufacture current page content. Answers and teacher information retain their owning chapter sources. Received chapter scripts are reused as immutable renderer support; the current platform entry overrides the native page-body path. Paragraph wrappers call that current entry. Stable chapter, paragraph, answer, teacher, complete-PDF and preview paths are preserved.

Actual page allocation is checked against designed-page markers and fixed chapter budgets. Text-bound checking traverses positioned placeholder children and compares the resulting text boxes with the authored bounds and all four physical page edges. The assembler resolves chapter-local destinations before appending and writes completed output bytes through temporary files before replacement. Exported references outside a paragraph slice explicitly point to the original chapter rather than losing their annotations.

The complete-PDF verifier passed independently: 110/57/19 pages, 180 assembled chapter pages with exact source text/pixels, 121 chapter-link rectangles and semantic destinations, overview endpoints 72/109, identical covers and cover geometry. The expanded paragraph verifier also passed 207 source/export page comparisons and 20 local/external paragraph links. All 31 figure specifications passed the 101 numerical checks.

## Repaired failure findings and reproduction proof

- **B1 — assembly could bless stale chapter PDFs.** Originally `--assemble` refreshed the source/chapter record around existing PDF bytes. It now validates the existing record without replacing it; only a successful complete rebuild refreshes provenance. In an external edition fixture, an actual manuscript was edited and the actual owning `--assemble` command was run. It failed with `Stale/unsafe native input`, returned nonzero, and left the record hash unchanged. Evidence: `independent-build-failure-probes.json`.
- **B2 — absolute page displacement escaped the overflow check.** Originally a short text box at `top:900pt`, `left:650pt` or `left:-200pt` passed while remaining on a one-page PDF. The new check rejects text beyond physical page edges. Independent probes verified rejection for right, left, bottom and top displacement. Four current native regression tests pass, including the overlong single-page-text case and the actual off-page cases. This closes the implementation gate left pending as N4 in the native-content report.
- **Actual native manuscript edit proof.** In the external copy of the real §2.1.1 manuscript, physical complete-book page 5 was changed from “alle flessen samen” to “alle kannen samen.” The owning `page_body`/CSS and chapter HTML renderer produced a one-page PDF with the changed heading and different pixels; actual layout validation passed. Both PDFs/PNGs and source hashes are bound in `actual-native-edit-proof/evidence.json`. No package or previous PDF was read as a build input. The altered wording is only a controlled external test, not a repository change.

The actual-source test used the author's intended local rendering environment: Python from the existing route `render-env`, `FONTCONFIG_FILE` pointing to its `fonts.conf`, and Pango/Cairo DLLs from `the configured native Pango/Cairo library directory`. A first probe without that font environment correctly failed the width gate because fallback glyph metrics differed; no repository source was changed to accommodate fallback fonts.

Independent checks at this checkpoint: 27 focused signed-authority/RX.4/generator Jest tests; 11 finite revision-inventory Jest tests; 4 native Python tests; authority derivation, outline currentness, durable lifecycle and live RX.4 CLI checks; the assembly/export/geometry checks above. Broad historical test fixtures, final source/output manifest closure, clean rebuild repeatability, paired compatibility and exact-head CI remain author/final-review deliverables and are not certified by these focused results.

## Portable evidence follow-up

This portable copy preserves the review findings. `PORTABLE-LINEAGE.json` records the original and portable hashes; environment/fixture paths have been replaced with relative labels. The original external reports are retained.

The previously running three focused historical lifecycle suites subsequently completed: 217 tests passed, zero failed. Their exact test-file hashes are in `independent-historical-test-addendum.json`. This is additional focused local evidence, not a remote CI verdict.

The final accepted-navigation ledger was independently compared directly with the supplied student and answer targets: all 77 + 44 source-page/destination pairs agree; stored rectangle rounding differs by at most 0.000458 point. The expanded current verifier passes using that ledger. `independent-accepted-navigation-check.json` and `independent-signed-verifier-final.json` bind those checks. The final LF normalization affects generated HTML/JSON only; the native-content binding inventory still matched after the rebuild. Lesson commit at this checkpoint is `f00a292d088e3bfed39f41bb4bf8a81963aa468f`; platform exact-head and CI review remain pending.
