# GATE-MTU-H2 Solo q1-q3 Micro-Case Gate Closure

Status: `pass_with_conditions`

Decision: PASS WITH CONDITIONS - routing only.

Closed on: 2026-05-27

Closure scope: routing only.

## Summary

GATE-MTU-H2 closes as `pass_with_conditions`. The gate accepts the Solo q1-q3
canonical micro-case package as sufficient routing evidence for a later bounded
CLI-mutation planning sprint.

Allowed next sprint: `MTU-H2A CLI-Mutation Planning`.

MTU-H2A may plan exact unit IDs, schema-valid CLI specs, command sequence,
rollback, audit log, and validation evidence for reviewed H2 lanes. MTU-H2A is
planning-only unless a later gate explicitly authorizes mutation execution.

## Approved For Later Bounded CLI-Mutation Planning

- q1 verbal external-cost recognition and explanation-with-example lanes.
- q2 pointwise TO, TVK from constant variable cost, unknown fixed costs from
  profit, and scale-factor handling lanes.
- q3 MO route split/supplement lanes, including non-calculus MO, derivative MO,
  `MO = gegeven MK`, and `MO = afgeleide MK`.
- q3 new-price-after-Q* and percentage-price-change-after-cost-change lanes.

## Deferred But Kept Visible

- q1 `Leg-uit-vraag met voorbeeld beantwoorden` is deferred to MTU-H4.
- q2 `Bereken-vraag beantwoorden` is deferred to MTU-H4.
- Broader tax/subsidy/cost-shock incidence and pass-through family work is
  deferred to MTU-H3.

## Conditions

1. GATE-MTU-H2 is a routing review only.
2. No protected reference mutation and no product use is authorized.
3. MTU-H2A is planning-only unless a later packet names exact unit IDs,
   schema-valid specs, CLI commands, rollback, audit log, and validation
   evidence.
4. q1 and q2 answer-form needs are deferred to MTU-H4 but remain visible.
5. D07 and broader pass-through/incidence work are routed to MTU-H3, not
   mutated from H2.
6. Guardrails remain binding: no full MPC/MSC requirement for q1, no full
   TO-function requirement for q2, no MK derivation or calculus-only MO route
   for q3.

## Refined Guardrail

Use this wording for q1:

```text
F16 may support q1, but q1 must not require full MPC/MSC machinery.
```

## Blocked Outcomes

- protected reference mutation;
- external-source mutation;
- machine-reference mutation;
- hand edits to `references/machine/`;
- hand edits to `references/external/`;
- unit minting;
- operation-registry mutation;
- answer-skill mutation;
- candidate writes;
- lesson-output mutation;
- target-exercise promotion;
- CP-6 closure;
- Year-1 closure;
- diagnostics;
- adaptive routing;
- mastery decisions;
- automatic sequencing;
- student-facing AI;
- summative use;
- PV projection;
- PV machine promotion;
- student/product use.

## Explicit Human Confirmation

The human reviewer supplied a complete answer set and confirmed closure as
`PASS WITH CONDITIONS - routing only` on 2026-05-27.

## Next Operational Step

Start `MTU-H2A CLI-Mutation Planning` as a bounded planning sprint. Do not
execute mutation, write candidate storage, mutate protected references, mutate
lesson output, or authorize product use from GATE-MTU-H2.
