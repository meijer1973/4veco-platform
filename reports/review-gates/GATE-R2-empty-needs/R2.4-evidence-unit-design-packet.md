# R2.4 Evidence And Unit-Design Packet

Sprint: R2.4  
Gate context: `GATE-R2-empty-needs` closed as `hold` in R2.3  
Status: review-ready packet, not mutation authorization

## Summary

R2.4 keeps Phase 2 bounded. It does not try to resolve every empty-needs or graph issue. It creates the decision packet needed before later CLI-only mutation work.

Main conclusions:

- Exact evidence is now attached to a small set of review-ready candidate edges.
- `D04 Elasticiteit en goederenclassificatie` is a unit-design problem, not a simple dependency-edge problem.
- Foundational A-domain units `A01`, `A02`, `A03`, `A04`, `A05`, and `A38` are ready to be classified as `underbouw_assumed` after workflow/CLI support.
- The suspicious machine suggestions `D10 -> D01/D03`, `D13 -> D03`, and `D32 -> D01/D03` remain rejected.
- Labor-market and unemployment sequencing has a bounded second-pass queue; it should not be mutated blindly from the R2.1 machine suggestions.

No protected reference data changed.

## Status Meanings

- `review_ready`: evidence is attached, but human review and CLI mutation are still required.
- `unit_design_required`: do not apply an edge until unit scope is changed or retired.
- `rejected`: do not apply this machine suggestion unless new evidence overturns it.
- `hold_for_later_phase`: useful issue, but outside the bounded R2.4 packet.

## Review-Ready Elasticity Edges

| Candidate | Status | Evidence | Next action |
|---|---|---|---|
| `A15` needs `A38` | `review_ready` | `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §2.1.1`, subquestions a-c require percentage changes before Ev. `references/machine/micro-teaching-units.json` shows A15 procedure uses percentage-change calculation. | Human review, then CLI mutation if accepted. |
| `A16` needs `A15` | `review_ready` | `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §2.1.3`, subquestion b and difficulty notes: cross elasticity uses same formula structure as Ev. R2.3 human interview Question 4 chose A15 as the route. | Human review, then CLI mutation if accepted. |
| `A17` needs `A15` | `review_ready` | `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §2.1.3`, subquestion a and difficulty notes: income elasticity uses same formula structure as Ev. R2.3 human interview Question 4 chose A15 as the route. | Human review, then CLI mutation if accepted. |
| `D06` needs `A15` | `review_ready` | `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §2.1.1`, required skills A15 and D06. `references/external/exam-questions.json`, `ha-1022-a-23-2-o`, opgave 1, q2 A15 and q4 D06. | Human review, then CLI mutation if accepted. |

## D04 Unit-Design Decision

`D04 Elasticiteit en goederenclassificatie` should not receive a simple `D04 -> A15` edge.

Decision: `unit_design_required`.

Reason:

- Goods classification belongs inside the relevant elasticity units.
- Price elasticity classification belongs with `A15`/`D06`.
- Income-elasticity classification belongs with `A17`.
- Cross-elasticity classification belongs with `A16`/`D27`.

Evidence:

- `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §2.1.3`: goods classification appears inside income and cross elasticity tasks.
- `references/external/exam-questions.json`, `ha-1022-a-25-2-o`, opgave 2, q8 uses `D04`; q9 immediately uses `A15` and `A17`.
- R2.3 human interview Question 5: human reviewer decided there is no need for a separate D04 unit.

Next action: later CLI-supported unit design change: retire, merge, or redistribute D04 content. Do not add a D04 prerequisite edge in R3.2.

## Rejected Market-Graph Machine Suggestions

These remain rejected:

| Candidate | Status | Reason |
|---|---|---|
| `D10` needs `D01/D03` | `rejected` | D01 and D03 are advanced accijns/surplus applications, not prerequisites for basic demand/supply graph-shift reasoning. |
| `D13` needs `D03` | `rejected` | D03 is too specialized to be a prerequisite for the general cost-increase supply-shift skill. |
| `D32` needs `D01/D03` | `rejected` | D32 is foundational graph interpretation; D01/D03 are later tax/surplus applications. |

Evidence:

- R2.3 human interview Question 6 explicitly rejected these suggestions.
- `references/authored/course-target-exercises.json`, `knowledge/course_blueprint_v4.md §1.2.2`, teaches shift-versus-movement reasoning before accijns/surplus work.

## Foundational A-Domain Empty Needs

Review-ready `underbouw_assumed` classification:

- `A01`
- `A02`
- `A03`
- `A04`
- `A05`
- `A38`

Evidence: R2.3 human interview Question 2 selected `underbouw_assumed` for foundational A-domain skills.

Held for later A-domain cleanup:

- `A18`
- `A42`
- `A43`

Reason: these are not safely covered by the simple foundational-underbouw rule. R2.2 evidence review flagged A18 and A42 because `prior_learning` is `new_this_year`, and A43 has suspicious suggested edges.

## Labor-Market And Unemployment Second Pass

This is a bounded second pass, not a full labor-market redesign.

Review-ready candidates:

| Candidate | Evidence | Notes |
|---|---|---|
| `L09` needs `L05` | `knowledge/course_blueprint_v4.md §4.1.2` defines labor-force categories; `§4.1.3` uses krappe/ruime arbeidsmarkt. | Good candidate after review. |
| `L11/L12/L13` need `L05` | `§4.1.2` defines werkloze beroepsbevolking; `§4.2.1` classifies unemployment types. | Good candidate after review. |
| `D16` needs `D34` and `L10` | `§4.2.2` says minimum wage is a direct transfer from minimum-price mechanics to labor market. Exam `ha-1022-a-25-1-o`, opgave 1, q3 assesses D16. | Candidate may need split into two concrete CLI edges. |
| `L17` needs `D16` | `§4.2.3`, subquestion a compares CAO wage-setting to minimum wage. | Good candidate after review. |
| `L19` needs `L09` | `§4.2.3`, subquestions c/e use loonstarheid to explain persistent unemployment and wage adjustment. | Good candidate after review. |
| `I04/I10` need `L19` | `§4.2.3` groups I04/I10 with L19; VWO exams `vw-1022-a-23-1-o` q22 and `vw-1022-a-25-2-o` q24 assess I10 wage/GA-curve rigidity reasoning. | Macro links need careful CLI coverage. |
| `H14` needs `I14` | H14 procedure references multiplier/leakages; VWO exam `vw-1022-a-23-2-o`, opgave 5, q21 assesses I14. | Already strongly supported by unit procedure and exam evidence. |

Held:

- `L09` needs `L03`: plausible, but current target-exercise evidence more directly supports `L05/L10`.
- `H13` needs `D13` or `D16`: plausible cost/minimum-wage prior reasoning, but the minimal prerequisite needs later review.

## Remaining Backlog Policy

Phase 2 is sufficient for now.

Do not expand R2.4 into:

- all 35 ambiguous empty-needs entries
- unit-term slug migration
- exam-question extraction gap closure
- blueprint missing-unit triage
- A18/A42/A43 targeted cleanup

Next sprint should be `R3.1 Reference CLI And Documentation Completion`.

`R3.2 Apply Reviewed Empty-Needs Corrections` remains blocked until:

- this packet is human-reviewed for mutation decisions
- R3.1 confirms the CLI/documentation/validator path
- accepted changes are applied through CLI or validated migration scripts only
