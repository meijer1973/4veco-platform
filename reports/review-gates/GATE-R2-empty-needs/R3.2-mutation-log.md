# R3.2 Mutation Log

Sprint: R3.2  
Status: `stopped_during_dry_run`  
Protected reference data changed: `false`

## Decision Source

- `reports/review-gates/GATE-R2-empty-needs/R2.4-mutation-review.json`
- `reports/review-gates/GATE-R2-empty-needs/R2.4-evidence-unit-design-packet.json`

## Dry-Run Results

The dry-run pass stopped at the first validation failure, before any protected reference write.

| Step | Command | Status | Output |
|---|---|---|---|
| 1 | `node build-scripts/references/unit-add-dep.js --dry-run --from A15 --to A38` | passed | `DRY RUN  A15 would depend on A38` |
| 2 | `node build-scripts/references/unit-add-dep.js --dry-run --from A16 --to A15` | failed | `A16: stored layer 1 below derived minimum 2` |

## Stop Reason

The current dependency-mutation CLI validates stored layer values before the dependency edit can safely recompute and persist derived layers. Adding `A16 -> A15` would require a layer update from the current stored value, and the command stops before writing.

This is a CLI workflow gap, not a license to hand-edit `references/machine/`.

## Applied Mutations

None.

## Required Next Action

Fix the unit dependency CLI so dependency mutations recompute and validate stored layers in the correct order, then rerun the R3.2 dry-run pass from the start.
