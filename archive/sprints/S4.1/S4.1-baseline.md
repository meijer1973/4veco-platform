# Sprint S4.1: Baseline

## Plan reference

`docs/sprints/S4.1-plan.md`

## Current repository state

S4.1 starts after `GATE-CP3-schema-extension-dry-run` closed as `pass_with_conditions`.

The current exercise overlay layer contains:

- one Tier A exam-question dry-run overlay
- one Tier C target-exercise dry-run overlay
- a provisional overlay schema
- a validator for basic overlay integrity

## Data integrity notes

No protected reference data is changed at baseline.

S4.1 must not hand-edit:

- `references/machine/`
- `references/external/`

S4.1 must not bulk-mutate `references/authored/course-target-exercises.json` and must not patch generated RAG chunks.

## Baseline risk

The CP-3 closure accepted the dry-run shape but identified risks that must be made explicit before scale:

- Tier A source annex/source-value gaps could be hidden during broad backfill.
- Provisional `exercise_operations` could be mistaken for a governed operation registry.
- Scaffolding scale values need reviewer calibration.
- `graph_spec` representation values are likely incomplete.
- `instructional_role: diagnostic` can be misread as product diagnostics permission.
