# Sprint R6.1: Baseline

## Plan reference

`docs/sprints/R6.1-plan.md`

## Baseline state

Current health reports exist primarily as Markdown under `reports/`. Some JSON reports already exist, but the required coverage reports do not yet have a uniform JSON-first contract.

Required R6.1 JSON outputs are not all present before the sprint.

## Data integrity notes

No protected reference data changes are allowed in R6.1.

`references/machine/` and `references/external/` are read-only inputs. Generated JSON reports are diagnostic outputs, not primary evidence.
