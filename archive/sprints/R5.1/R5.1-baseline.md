# Sprint R5.1: Baseline

## Plan reference

`docs/sprints/R5.1-plan.md`

## Baseline summary

R5.1 starts after R4.3 completed blueprint flag triage.

Baseline observations:

- `references/schemas/evidence-anchor.schema.json` exists but is minimal.
- `references/schemas/claim.schema.json` exists but is minimal.
- `references/data/evidence-anchors.json` does not exist yet.
- `build-scripts/references/check-evidence-anchors.js` does not exist yet.
- High-risk evidence currently lives inside review packets and generated reports, not a governed anchor layer.

## Data integrity notes

This is a non-mutating sprint for protected references. `references/machine/` and `references/external/` remain read-only. R5.1 creates governance data, schema contracts, and generated reports.

## Known risks

- Generated reports are useful discovery surfaces but cannot become primary evidence.
- Some source locators are still coarse because exact PDF page/line extraction is not yet a general evidence-anchor pipeline.
- R5.1 must not build the alignment graph; it only prepares anchors and claims for R5.2/R5.3.
