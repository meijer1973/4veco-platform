# COMPANION-WORKFLOW-GUARDRAILS-1 Post-#209 Plan Review

Reviewer: Rawls (`019ffaf1-c6b8-7152-88ff-d66366207ccb`)

## Round 1

Verdict: `REVISE`.

- Bind implementation review to an exact committed substantive SHA.
- Classify exact-head remote CI before compatibility and publish aligned
  workflow/artifact/state evidence before readiness.
- Generate the final four-file index tail from trusted-main tooling with
  explicit source commits, deterministic proof, and no later commit.
- Do not conflate immutable payload compatibility with the later authorized
  lesson-first integration refresh.

## Round 2

Verdict: `OK`.

The revised plan implements every finding, re-fetches both repositories before
proof-sensitive transitions, and explicitly excludes bundle authorization and
merge.

## Generated-Tail Clarification

Verdict: `OK`.

Rawls confirmed that the pre-readiness terminal tail may contain only the
generated index files with byte-level deltas. All four indexes must still be
regenerated and verified from trusted-main tooling. In this refresh, the lesson
source and canonical timestamp are unchanged, so the lesson pair remains
byte-identical and the terminal commit must contain only the changed platform
JSON and Markdown pair. The exact four-path delta remains a requirement for the
later post-lesson-merge `integration_refresh`; no artificial lesson delta is
permitted.
