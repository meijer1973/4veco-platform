# COMPANION-WORKFLOW-GUARDRAILS-1 Post-#210 Plan Review

Reviewer: Rawls (`019ffaf1-c6b8-7152-88ff-d66366207ccb`)

## Round 1

Verdict: `REVISE`.

- Bind platform index provenance to the committed lead-review evidence SHA,
  which must be the direct parent of the terminal generated tail.
- Pin generator/checker tooling to trusted platform `main` and prohibit any
  commit after the tail.
- Verify the immutable lesson-first integration contract without claiming a
  runtime refresh before the lesson merge exists.
- Re-fetch both mains and both PRs before every proof boundary.
- Specify the later canonical bundle authorization and trusted lesson-first
  integration behavior.

## Round 2

Verdict: `REVISE`.

- Restore identical exact bundle metadata and supersession records in both PRs.
- Require both readiness decisions to be `READY_FOR_HUMAN_REVIEW` with
  coordinated `MARK_READY`, while preserving `merge_ready: false`.
- Use only the five canonical bundle invalidation identifiers.

## Round 3

Verdict: `REVISE`.

- Keep exact base SHAs in surrounding evidence, not in the canonical machine
  authorization record. The integration lane re-fetches current bases.

## Round 4

Verdict: `OK`.

The accepted plan binds every proof to fresh remote state, preserves the
stronger PR #210 regression, makes committed review evidence the final index
source/direct parent, requires replacement lesson-first compatibility, mirrors
exact evidence onto both PRs, applies coordinated readiness only when both
members permit it, and stops before merge pending one canonical human bundle
authorization.
