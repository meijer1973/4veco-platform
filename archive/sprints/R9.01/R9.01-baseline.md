# Sprint R9.01: Baseline

## Plan reference

`docs/sprints/R9.01-plan.md`

## Baseline summary

Repo-local directories with `module-3` names before the sprint:

- `source-data/module-3/`
- `source-data/module-3/reasoning/`
- `source-data/module-3/skilltree/`
- `build-scripts/content/module-3/`

These are repo-local legacy support folders. The external frozen lesson target may still carry its historical name; this sprint does not rename that external target.

## Data integrity notes

No protected reference data changes are allowed. `references/machine/` and `references/external/` remain read-only.
