# Sprint R0.2: Baseline

## Plan reference

`docs/sprints/R0.2-plan.md`

## Baseline summary

R0.2 starts with no source manifest or document inventory under `references/data/`.

The sprint scaffold exists:

- `docs/sprints/README.md`
- `docs/sprints/example-plan.md`
- `reports/sprints/example-result.md`
- `build-scripts/sprints/check-sprint-plan.js`
- `build-scripts/sprints/check-sprint-result.js`

## Current inventory gap

Before implementation, these files are missing:

- `references/data/source_manifest.json`
- `references/data/document_inventory.json`
- `build-scripts/references/build-reference-inventory.js`
- `build-scripts/references/check-source-manifest.js`
- `build-scripts/references/check-document-inventory.js`

## Baseline file surfaces

The sprint must inventory:

- `references/`
- `reports/`
- `build-scripts/references/`
- `build-scripts/reports/`

## Data integrity notes

No protected reference data has been changed at baseline. `references/machine/` and `references/external/` are protected surfaces and must remain unchanged by this sprint.

## Known risks

- The inventory JSON files live under `references/data/`, so the inventory must handle self-generated inventory files without unstable self-checksums.
- Reports under `reports/` are generated views and must be classified as generated projections, not source evidence.
- CLI documentation currently lags the implemented script folder and should be recorded as a future documentation issue, not fixed inside this inventory sprint unless needed for classification.
